# OpenClaw handoff — the manager/worker AI system on Krutesh's Mac

Written 2026-09-20 at the end of the session that built it. Read this **before touching anything OpenClaw-related**. It records what exists, why each piece is shaped the way it is, every trap already hit (several of them by me, the assistant), and what is still to do.

Audience: the next Claude Code session, and the owner. The owner is a **no-code founder** — explain in plain language, lead with the answer, never assume he can edit code or config himself.

---

## 1. What this is, in one paragraph

OpenClaw runs on the Mac as a local "AI employer": a **manager** AI plans work and hands tasks to **worker** AIs, reviews what comes back, and reports. It runs on the owner's two subscriptions — **ChatGPT Plus** (GPT-6 Astra, via the Codex harness) and **Claude Pro** (Claude models, by driving the Claude Code CLI already installed) — with **no API keys and no pay-per-use billing**. Its first job is auditing the chapter அ Meta integration against the offline Meta docs. It is **read-only**: it never pushes, deploys, touches the live database, or edits the owner's real project folder.

**Status: built, fenced, fence-tested, and end-to-end tested — the full manager → researcher → co-manager chain ran green on 2026-09-20 (§9 to-do 1).** Not yet moved to a server, and **on hold there by the owner's decision** (§9 to-do 5).

> ### ⚠ START HERE — what changed on 2026-09-21 (third session)
> Four real defects found and fixed. **No job was run**; all of this is setup. Nothing committed, nothing pushed.
> 1. **THE WORKERS HAD NO STANDING RULES AT ALL — the biggest find.** Each agent loads `AGENTS.md` from its **own** workspace (confirmed against OpenClaw's docs), and all four workers point at `~/OpenClaw-Workspace/chapter-copy`, **which had no `AGENTS.md`**. So researcher, researcher2, verifier and comanager have always run blind, and the manager pasting the boilerplate was the *only* thing that ever told them the rules — a silent failure every time it forgot. **Fixed:** `~/OpenClaw-Workspace/chapter-copy/AGENTS.md` now exists (2,486 chars: the 8 programs, one-command-per-call, no `pwd`, quote spaced paths, the both-halves-of-evidence rule, the reply cap, and never-widen-the-task). **Proven loaded** by a nonce planted at the very END of the file (`CHAPTER-WORKER-RULES-7Q4M`): a researcher told to use no tools returned it verbatim, which also proves the whole file arrives untruncated. Keep pasting the boilerplate as well — belt and braces.
> 2. **That file would have been deleted on the very next refresh.** `chapter-refresh-copy` is `rsync -a --delete` and the real project has no `AGENTS.md`. **Fixed:** added an anchored `--exclude '/AGENTS.md'` (leading slash, so it protects only the root file and would not hide a nested one), documented in the script's own comment block. Backup: `~/.local/bin/chapter-refresh-copy.before-agents-md-exclude`. Proven by running a refresh: 648 files, the file survived.
> 3. **The manager's `AGENTS.md` was 2,261 characters from silently losing the owner's rules.** It was **17,739 chars against the 20,000 default `bootstrapMaxChars`**, and bootstrap files truncate from the END — where the whole chapter அ section sat. **Fixed twice over:** `agents.defaults.bootstrapMaxChars` raised **20000 → 32000** (nothing was truncated at the time, so this cost nothing and only removed the cliff), **and the file was reordered** so the chapter அ section is now FIRST and OpenClaw's stock template last, with a precedence note at the top. Any future truncation therefore eats generic boilerplate instead of the owner's rules. Backup: `~/.openclaw/workspace/AGENTS.md.before-reorder-2026-09-21`. Verified no line was lost.
>    - **Why it was shaped that way, for the record:** OpenClaw ships a stock `AGENTS.md` with a new workspace (6,312 chars of generic personal-assistant guidance — Group Chats, Automations, Make It Yours). Whoever set this up **appended** the chapter rules below it instead of replacing it. Nothing was wrong with that at 12k chars; it became a hazard as the file grew. **The stock half is still there, still generic, and is the obvious thing to trim if the file ever needs to shrink** — not the owner's rules.
> 4. **The §6 "No file writing" row was wrong as written** (flagged by the first real job, never fixed). The manager writes by design; the fence was always about workers. Row reworded, and the fence **re-proven unattended**.
> 5. **Verified live 2026-09-21, so the next session need not redo it:** `openclaw security audit` = **0 critical, 3 warn**, the same three known ones, so two days of config changes introduced nothing new · the researcher lane works on Luna · **a worker can read the Meta doc sets at their spaced absolute paths outside its workspace** (`/Users/krutesh/Downloads/Conversions API Docs`), which every Meta audit depends on and which had never been tested · both automations show a **stale red "error"** in the dashboard from the 2026-09-20 plugin incident, but `consecutiveErrors: 0` and trigger evaluation is current — that is trap 25, not a fault.
> 6. **Telegram delivery PROVEN for the first time** (§7b) — watcher detected, payload ran, message delivered. Also settled: **cron payloads are NOT bound by the exec allowlist, only trigger scripts are** — a prediction this session got wrong, recorded in §7b with the reasoning.
> 7. **A small CAPI pilot job is drafted and ready** — §9 to-do 11a. It was NOT run.
> 8. **`chapter-budget` added — the overnight stall nobody had hit yet.** `AGENTS.md` told the manager to run **`openclaw models status`** before each research phase, but **`openclaw` is NOT on the exec allowlist**, so every budget check raised an approval prompt. Attended, the owner just clicked allow; unattended it burns ~90 seconds and fails, **before every phase**. Fixed the way this system already solves this: a wrapper, `~/.local/bin/chapter-budget`, which takes no arguments and prints only the `usage:` line — never the auth-profile block, which carries account emails and state-store paths. Allowlist is now **9 programs**. **`openclaw` itself must never be allowlisted**: the allowlist approves whole programs with any arguments (trap 7), so that would hand every agent the CLI that rewrites config, rotates the gateway token and manages automations. **Proven unattended 2026-09-21:** a researcher ran `chapter-budget` with nobody approving and got the figure, no prompt. `AGENTS.md` updated (backup `AGENTS.md.before-chapter-budget`).
> 9. **A stock-template line told the manager to push. Removed.** The generic half of `AGENTS.md` listed as proactive work needing no permission: *"check projects (`git status`, etc.); commit and push your own changes."* That directly contradicts golden rule 1, and `git status` would also stall on the allowlist. The 2026-09-21 reorder already put chapter அ first with a precedence note, but **a "you may push without asking" sentence is not something to leave to precedence** — it is deleted, with a note recording what was there and why. **Lesson for anyone importing stock config: read the generic half for instructions that contradict the owner's rules, do not assume the override covers it.**
>
> ### ⚠ START HERE if you are a new session — what changed on 2026-09-20 (second session)
> Tuning only; **no real work was run after the safety review**. **This session committed and pushed nothing**; `OPENCLAW-HANDOFF.md` and `.claude/skills/openclaw-fixes/` are still uncommitted. (Commits *did* appear during the day from the owner's other sessions — don't assume a changed commit count means OpenClaw did it; it has no git at all.)
> 1. **A real hole was found and closed** — `sort` and `uniq` could write files; the allowlist is now **8 programs** (§4, and the ⚠ in §6).
> 2. **The first real job ran** — the OpenClaw safety review; verdict **PARTIALLY COMPLIANT** (§9 to-do 3).
> 3. **The researcher moved from Claude Sonnet 5 to GPT-5.6 Luna** after a scored bake-off (§2), to stop Claude Pro — which is shared with the owner's own sessions — being the bottleneck.
> 4. **Thinking levels set for the first time**: researcher/manager `medium`, verifier/co-manager `high` (§2).
> 5. **The heartbeat was costing 4–6% of the ChatGPT 5-hour window *per run*.** Root cause was `isolatedSession` being unset (a known upstream bug). Now fixed along with model, interval, context and prompt (§7).
> 6. **`AGENTS.md` was edited** (researcher model in two places, and the worker boilerplate, which was still telling workers to use the now-removed `sort`/`uniq` and did not forbid `&&`). Backup: `~/.openclaw/workspace/AGENTS.md.before-luna-switch`.
> 7. **LATER THE SAME EVENING a ChatGPT session rebuilt the resume mechanism** — it disabled the recurring heartbeat and installed a model-free cron gate (`chapter-auto-resume`). **Its design is better than the heartbeat it replaced and should be kept**, but it stopped mid-validation when its own allowance ran out, so parts are unverified (§7a, §9 to-dos 13–14).
> 8. **Telegram channel is LIVE (2026-09-20) — OpenClaw can finally talk to the owner.** Bot `@openclawkrutesh_bot`; status reads `enabled, configured, running, connected, mode:polling`. **Locked to the owner alone: `dmPolicy: allowlist`, `allowFrom: ["8155091226"]`, `groupPolicy: disabled`.** The raw bot token is **not** in `openclaw.json` — the stored `channels.telegram.botToken` is a 21-char reference to a credential held elsewhere, so it does not ride along in config snapshots (relevant to trap 2's token-leak history). **`message` stays globally denied**, so agents still cannot spontaneously message anyone; notifications travel by *scheduler delivery* instead, which is the correct separation. `chapter-auto-resume` now delivers `{mode: announce, channel: telegram, to: 8155091226, bestEffort: true}` — **`bestEffort` matters: a delivery failure must never kill a resumed audit**, which is the lesson of the old `no-route` receipt. Setup gotchas are traps 23–24. Config backup: `~/.openclaw/openclaw.json.before-telegram-2026-09-20`.
>    - **The plugin was BLOCKED by `plugins.allow` and had to be added** (42 entries now). A channel that is enabled but not allowlisted fails silently.
>    - **Declined deliberately: "Administration from chat."** It would grant update/restart/config-change/**command-approval** from Telegram and works "in servers and groups without DM pairing" — i.e. anyone reaching the owner's Telegram could approve the very exec prompts that correctly blocked `touch` (trap 21). Add later only if approvals-from-phone is genuinely wanted.
> 8b. **SUPERSEDED — original note:** Telegram channel started, NOT finished. Plugin `telegram` (stock, v2026.9.5) was **blocked by `plugins.allow` and had to be added** — it is now allowed and enabled, with `dmPolicy: pairing` and `groupPolicy: disabled` set *before* any token existed so the bot is never briefly open. **The token still needs re-entering** (traps 23–24 explain why it was lost). Once paired, switch to `dmPolicy: allowlist` + `allowFrom: ["<user id>"]`, read from `openclaw pairing list telegram` rather than a third-party id bot. **Delivery is currently `no-route`: OpenClaw cannot tell the owner anything** — reports land silently on disk. Config backup: `~/.openclaw/openclaw.json.before-telegram-2026-09-20`.
> ### Live state as of 2026-09-21 (verify, don't trust)
> | Piece | State |
> |---|---|
> | Manager | `main` · GPT-6 Astra · thinking **medium** · ChatGPT |
> | Researcher A | `researcher` · GPT-5.6 Luna · medium · ChatGPT |
> | Researcher B | `researcher2` · Claude Sonnet 5 · medium · Claude — **added 2026-09-21** |
> | Verifier | `verifier` · Claude Opus 4.7 · **high** · Claude |
> | Co-manager | `comanager` · Claude Opus 5 · **high** · Claude · **PLAN REVIEW ONLY** |
> | Sub-agents | `maxConcurrent: 3`, allowAgents = researcher, verifier, comanager, researcher2 |
> | Exec allowlist | **8 programs** (no `sort`/`uniq`/`pwd`/`sqlite3`/`git`) |
> | Heartbeat | `every: 0m` — **disabled**, replaced by the resume gate (§7a) |
> | Automations | `chapter-auto-resume` (5m) · `report-ready-notify` (5m) · `gate-health-check` **DISABLED, unbuildable** |
> | Background learning | **OFF by owner decision (2026-09-21):** Skill Workshop autonomous mode is `off`; Memory Dreaming is disabled. All five weekly `skill-collection-review` definitions are disabled, and the managed Memory Dreaming job is absent. Manual learning remains available when explicitly requested. |
> | Telegram | live, `@openclawkrutesh_bot`, allowlist = owner only |
>
> ### ⚠ What has NEVER been proven (read before trusting any of it)
> 1. **The resume gate has never fired for real.** Its decision logic is 16/16 in a harness; the half that spends money — a genuine Astra handoff resuming real work — is untested.
> 2. **The manager has never written `resume-state.json`.** AGENTS.md and the audit prompt both instruct it; neither has executed.
> 3. **The single-provider rules (§7a) have never run.** No job has parked UNVERIFIED CANDIDATES and resumed.
> 4. ~~**Telegram delivery has never actually delivered.**~~ **PROVEN 2026-09-21, end to end** — see §7b. `report-ready-notify` detected a new report, ran its payload (`lastRunStatus: ok`) and Telegram accepted the message (`lastDeliveryStatus: delivered`, `lastDeliveryError: None`).
> 5. ~~**The §6 fence tests were not re-run** after two days of config changes (to-do 8).~~ **DONE 2026-09-21:** `security audit` clean (0 critical / 3 known warns), worker read proven, and the worker **write** fence re-proven unattended — `touch` prompted, nobody approved, no file created.
> **The next real job is therefore the first real test of nearly all of it. Expect to watch it, not trust it.**
>
> ~~**Before running anything real, do §9 to-do 8**~~ — **done 2026-09-21.** Items 1–4 above remain unproven and the next real job is still their first test.

---

## 2. The team

| Role | agent id | Model | Plan | Job |
|---|---|---|---|---|
| Manager | `main` | `openai/gpt-6-astra` | ChatGPT Plus | Plans, delegates, keeps progress, writes the report |
| Co-manager | `comanager` | `anthropic/claude-opus-5` | Claude Pro | **Reviews the PLAN only** (trimmed from 3 points, 2026-09-21) |
| Researcher | `researcher` | `openai/gpt-5.6-luna` | ChatGPT Plus | The bulk reading: code vs docs, candidate findings |
| Researcher B | `researcher2` | `anthropic/claude-sonnet-5` | Claude Pro | Same job as A, different AI company — added 2026-09-21 |
| Verifier | `verifier` | `claude-cli/claude-opus-4-7` | Claude Pro | Tries to **disprove** each finding |

**The structure was checked against published 2026 evidence on 2026-09-21. Verdict: sound, with one role on watch.**
- **Do NOT downgrade the manager to Sonnet.** The clearest finding in the literature: *"You cannot cheap out on the orchestrator while using cheaper models for workers — it's a false economy"*; delegation *"works reliably only on the two strongest models"* because *"the orchestrator capability has a floor."* **Strong orchestrator + cheap workers is the winning pattern** — one benchmark kept **96% of an all-frontier team's score at 46% of the cost**. Astra stays. (The owner asked about this twice; the manager is on `thinkingDefault: medium`, never high — that worry was unfounded.)
- **The verifier is the best-founded role.** Guidance says a judge should be *"at least as strong as the generator, from a different family if possible"*, and **Opus 4.7 is specifically named as one of 2026's two default frontier judges**. Cheap-reader + strong-judge tiering is documented to cut cost **59.4% for identical verified results**. Do not touch it.
- **The co-manager was TRIMMED to plan-review only (owner's decision, 2026-09-21), not removed.** The asymmetry is the point: **a wrong plan is the one mistake nothing downstream can repair**, because the verifier only checks claims that *were* made, never the ones nobody thought to look for — so a badly-framed plan silently spends a whole job's budget on the wrong question, and no later check can catch it. Verdicts are the opposite: **the verifier already re-checks every finding from a different AI company**, so a second opinion there was duplicated work, and the first real job showed it (agreement on all ten verdicts — the textbook *diversity-collapse* signal). Recommended fixes reach the owner regardless; he is the second opinion. Net effect: the co-manager costs roughly **one turn per job** instead of several, Opus 5 largely leaves the scarce Claude pool, and the team is close to 2 GPT / 2 Claude — which also eases the rate-limit concentration that bit twice on 2026-09-20. **AGENTS.md still requires the manager to record AGREED/DISAGREED in the report; keep tracking that ratio.** If the co-manager never disagrees on plans across three jobs either, removing it entirely becomes defensible.
- **⚠ Original note (the research behind that decision):** Findings on debate are mixed (*"a well-prompted single model does about as well"*; *"debate and base-model quality may be substitutes"*), and there is a named failure mode, **"debate diversity collapse": when disagreement trends to zero, the vote no longer carries information.** **In the one real job so far it agreed on all ten verdicts** — exactly that signal, from one sample. The counter-argument is real (cross-family checks catch errors same-family ones miss). **Record agree/disagree per decision point; if it never disagrees across three jobs, it is a paid rubber stamp.**
- **Two researchers, not more, and on DIFFERENT families.** *"Multiple competent but individually limited agents can outperform a single stronger agent when they draw on diverse models"* — but *"adding more agents yields little when systems are homogeneous, as correlated outputs produce diminishing returns."* A second Luna would buy almost nothing; Sonnet 5 buys real diversity.
  **⚠ CORRECTION — a second researcher is NOT failover, and an earlier note here wrongly implied it was.** The **manager runs on ChatGPT too**, from the same allowance as Researcher A. If ChatGPT is exhausted the manager stops, and **a stopped manager cannot delegate anything to Researcher B** — it simply idles. That is precisely what happened on 2026-09-20: ChatGPT ran dry and the researcher *and* the Telegram bot both went down, because both depend on Astra. **The manager is the single point of failure and a second researcher does not change that.** What actually recovers from an exhausted limit is the **resume gate** (§7a). The two genuine benefits of Researcher B are (1) diversity of error modes, and (2) **ChatGPT burns roughly half as fast**, because the manager no longer pays for all the reading — so the limit arrives later, or not at all. Prevention, not recovery.
  **AGENTS.md now tells the manager to protect its own budget**: check `openclaw models status` before each research phase, and once the ChatGPT 5-hour allowance falls below about a third, route new research to Researcher B and keep the remainder for its own orchestration turns — since the manager is the one role nothing else can perform. `maxConcurrent` raised **2 → 3** (OpenClaw's own default is 8) so the breadth-first phases can actually parallelise.
- **Researcher B verified live on 2026-09-21: Sonnet 5 scored 20/20** on the same three-question benchmark (correct `API_VERSION` line 60 and not fooled by the version numbers in nearby comments, all 10 exports with correct lines, 5 real importers, and it caught the `meta-signal-watchdog` comment-only decoy). It also volunteered the *use* site at line 396, which is correct. **One demerit: it exceeded the 14-line reply cap** — relevant because over-long worker replies are truncated before the manager sees them (trap 18); Luna answered in 9 lines. **Full bake-off is now a four-way tie on quality — Luna, Sol, Terra and Sonnet 5 all 20/20 — so choose on cost, speed and provider balance, not accuracy.**
- **Sonnet 5 over Sonnet 4.6, deliberately.** In this install `claude-cli/claude-sonnet-4-6` is capped at **200k context vs Sonnet 5's 1M** (the public spec claims 1M for both — the local catalog is what counts), and 4.6 is reported to *"often not finish"* complex multi-step jobs, which is exactly a researcher's shape. Honest cost caveat: **Sonnet 5's tokenizer emits ~30% more tokens for the same text**, and the intro pricing that offset that ended 31 Aug 2026.
- **The premium is real.** Anthropic's own figure for full orchestrator-worker topology is **~15x tokens**, and it *"only wins when the task decomposes into independent parallel threads."* Phase B of the CAPI audit (surveying 193 doc pages) is that shape; Phase A is narrower and may not need the whole team.

**Why this split (revised 2026-09-20 — the researcher moved from Sonnet 5 to GPT-5.6 Luna):** **ChatGPT Plus has ONE claimant — OpenClaw's manager. Claude Pro has FOUR — researcher, verifier, co-manager AND the owner's own Claude Code sessions.** So the target is not a 50/50 split; it is *push volume onto ChatGPT, because Claude has other jobs*. The researcher carries the volume ("most work goes here"), so it belongs on the idle subscription. Verifier and co-manager stay on Claude deliberately: the verifier because its 6/6-vs-5/6 bake-off win is *measured*, and the co-manager because the two-manager rule is worthless if both managers are the same AI company.
- **Researcher bake-off, 2026-09-20 — Luna / Sol / Terra all scored 20/20; Luna was cheapest.** Task: three questions on `_shared/metaCapi.ts` with ground truth computed independently — (A) the real `API_VERSION` line (line 60, with v21/v25/v26 mentioned in nearby comments as distractors), (B) all 10 exports with line numbers, (C) which files *really* import it, where `meta-signal-watchdog` is a **decoy that only mentions the filename in comments**. Results: **Luna 20/20, 54s, <1% of the 5-hour window · Sol 20/20, 38s, ~1% · Terra 20/20, 125s, ~1%** (Terra's time includes recovering from a denied compound command). All three caught the decoy. The Sonnet control aborted and was **not** re-run, deliberately: three models already scored full marks, so Sonnet could only tie, and a tie cannot argue for keeping volume on the scarcer pool.
- **Caveat: a test everyone passes does not discriminate.** This proved competence and relative cost, not a quality ceiling. Real audit work (reading long docs and synthesising) is harder than grep-and-cite. Watch Luna's output in the first real audit; the Opus 4.7 verifier is the designed safety net if a cheap researcher gets sloppy.
- **Thinking level is a SEPARATE knob from the model, and it was unset on every agent until 2026-09-20.** `agents.entries.<id>.thinkingDefault` takes `off · minimal · low · medium · high · xhigh · adaptive · max · ultra`. Now set: **researcher `medium` · manager `medium` · verifier `high` · co-manager `high`.**
  **Why, from published 2026 benchmarks:** high vs medium is nearly a wash on general work (**65% vs 64% success for roughly double the cost**), and gains of 2–5 points cost **4–5× the tokens** — so high is waste on volume roles. **But for *verification* specifically, high does beat medium**, which is why the verifier and co-manager (both low-volume, high-stakes judgement) get it while the researcher (high-volume) does not. Local evidence agrees: Luna scored **20/20 at the inherited default**, so spending 4–5× more tokens there would buy nothing.
  **Caveats:** those benchmarks are GPT-5-family on math/code/general suites, **not** our doc-audit task; and OpenClaw's nine levels do not map 1:1 onto the three-tier "reasoning effort" those studies measured. Treat this as informed defaults, not proof — and **do not reach for `xhigh`/`max`/`ultra`**, which is the far end of a curve that already flattened at `high`.
  **The heartbeat cannot have its own level** — it runs *as* agent `main`, so it inherits `main`'s `medium`. Cheapening it further would mean lowering the manager's planning quality, which is not worth it; `lightContext: true` and the Luna switch already carry that saving.
- **Switching the researcher model requires FOUR edits, not one** (trap 4, plus AGENTS.md overriding config): `agents.entries.researcher.model`, `agents.entries.researcher.subagents.model`, the **team table in AGENTS.md**, and — most easily missed — **the explicit spawn line in AGENTS.md** ("Always spawn with … researcher → `<model>`"), because the manager passes that model explicitly and an explicit argument beats config.

**The two-manager rule** (in `AGENTS.md`): **TRIMMED 2026-09-21 to ONE point — the plan, before research starts.** It used to be three (plan, verdicts, recommended fix). Each writes its position **independently first**, then one round of rebuttal. Still disagreeing → **escalate to the owner with both positions**, never split the difference, never keep arguing.

**Finding labels:** COMPLIANT / PARTIALLY COMPLIANT / NON-COMPLIANT / OBSOLETE / IRRELEVANT / UNCERTAIN. Verifier verdicts: CONFIRMED / REJECTED / UNCERTAIN. Every finding needs the doc file + quoted line **and** the code `file:line`; without both it is not a finding.

---

## 3. How the owner uses it

- **Jobs** (audits, research): typed into the **OpenClaw dashboard** — `openclaw dashboard`, or the Mac app. It is local-only at `http://127.0.0.1:18789`.
- **Setup changes, code fixes, reading reports**: in his Claude Code session (this app).
- **Reports** land in `~/.openclaw/workspace/reports/` as `<date>-<job>.md`, with a `<date>-<job>-progress.md` updated after each area.
- **Before starting a job, check the budget — it is free:** `openclaw models status` prints e.g. `openai usage: 5h 0% left ⏱3m · Week 84% left ⏱6d 19h`.
- **⚠ `openclaw models status` SHOWS ONLY THE ChatGPT BUDGET. It is blind to the Claude one** — it lists `Providers w/ OAuth/tokens (1): openai`, because the Claude workers run through the `claude-cli` backend on the owner's Claude Pro login, which OpenClaw never meters. **The Claude Pro allowance is shared by the researcher, the verifier, the co-manager AND the owner's own Claude Code sessions**, so an interactive session watching a job is *competing with the very workers doing it*, not merely adding overhead. Measured 2026-09-20 at the end of the safety review: ChatGPT 5-hour **46% used**, Claude Pro 5-hour **96% used**. Read the Claude side from the Claude Code app's own usage card (or `get_usage`), never from `openclaw models status`.
- **Therefore: type routine jobs straight into the OpenClaw dashboard and walk away.** The job writes `-progress.md` after every area and a final report, and the heartbeat resumes it after a limit — live supervision by a Claude session buys nothing for the job's outcome and costs the workers their budget. Bring a job back to an interactive Claude session only for the things OpenClaw structurally cannot do: run a command to settle an UNCERTAIN finding, change config, or write to the real repo.

---

## 4. Live configuration (verified 2026-09-20)

Config file `~/.openclaw/openclaw.json`. Backups made along the way: `.before-fences`, `.before-roles`, `.before-copy-and-comanager`.

```
agents.defaults.model.primary          openai/gpt-6-astra
agents.defaults.subagents              model=openai/gpt-5.6-luna, maxConcurrent=2,
                                       allowAgents=[researcher, verifier, comanager], requireAgentId=true
agents.defaults.skipBootstrap          true
agents.defaults.heartbeat              model=openai/gpt-5.6-luna, every=2h, lightContext=true,
                                       isolatedSession=true, agentId=main, prompt=(1000 chars,
                                       fast-path rewrite — §7)
agents.entries.main                    name "Manager — GPT-6 Astra" (was unnamed, so the dashboard
                                       showed the default label "Assistant"), thinkingDefault=medium
thinkingDefault per agent              main medium · researcher medium · verifier high · comanager high
agents.entries.researcher              model openai/gpt-5.6-luna · ws ~/OpenClaw-Workspace/chapter-copy
agents.entries.verifier                model claude-cli/claude-opus-4-7 · ws ~/OpenClaw-Workspace/chapter-copy
agents.entries.comanager               model anthropic/claude-opus-5 · ws ~/OpenClaw-Workspace/chapter-copy
   each of the three: tools.deny [write, edit, apply_patch, web_search, web_fetch, x_search]
                      tools.exec.mode "ask"
                      subagents.model pinned to its own model  (see trap 4)
tools.profile                          coding
tools.deny                             gateway, plugins, codex_plugins, secrets, automations, terminal,
                                       computer, nodes, mobile_ui, message, portal, browser, screen,
                                       web_search, web_fetch, x_search
tools.exec                             host gateway, mode ask   (= allowlist + ask on-miss + askFallback deny)
tools.sessions.visibility              tree
tools.agentToAgent.allow               [main, researcher, verifier, comanager]
tools.elevated.enabled                 false
env.vars.ENABLE_CLAUDEAI_MCP_SERVERS   "false"   (string, not boolean — see trap 9)
plugins.allow                          the 41 plugin ids that were enabled (blocks anything new loading)
gateway.mode                           local (loopback 127.0.0.1:18789)
browser plugin                         disabled
```

**Exec allowlist** (agent `*`, via `openclaw approvals get`) — **8 programs since 2026-09-20**: `/Users/krutesh/.local/bin/chapter-check`, `/Users/krutesh/.local/bin/chapter-refresh-copy`, `/bin/cat`, `/usr/bin/head`, `/usr/bin/tail`, `/bin/ls`, `/usr/bin/wc`, `/usr/bin/grep`. **`/usr/bin/sort` and `/usr/bin/uniq` were REMOVED** after a worker used `sort -o` to write a file — see the ⚠ in §6. Every entry is registered "any args", so vet a program's flags for a write path or shell escape before adding it.

**Deliberately NOT allowlisted:** `sed` (`-i` writes), `awk` (`system()`), `find` (`-delete`, `-exec`), `rg`/`ugrep` (`--pre`, `--filter` run commands), `xargs`, `pwd`, and **raw `git`**. `chapter-git` (an earlier commit wrapper) was **removed** from the allowlist when OpenClaw moved onto the copy — agents no longer need or have any write path to the real repo.

**Key paths**
| What | Where |
|---|---|
| OpenClaw CLI (not on PATH) | `~/.openclaw/bin/openclaw` |
| Manager instructions | `~/.openclaw/workspace/AGENTS.md` (chapter section at the end) |
| Reports + progress files | `~/.openclaw/workspace/reports/` |
| **The copy OpenClaw works on** | `~/OpenClaw-Workspace/chapter-copy` (644 files, 59 MB) |
| The owner's real project (**off limits to OpenClaw**) | `~/Downloads/Website Flow Multi-Pickup` |
| Offline Meta docs (read-only reference) | `~/Downloads/{Meta Pixel Docs, Conversions API Docs, Marketing API Docs, API Reference Docs}` |
| Helper scripts | `~/.local/bin/chapter-check`, `~/.local/bin/chapter-refresh-copy` |
| Mac app | `/Applications/OpenClaw.app` (2026.9.5, genuine: OpenClaw Foundation, notarized) |

---

## 5. The copy, and why it exists

`chapter-refresh-copy` rsyncs the real project → `~/OpenClaw-Workspace/chapter-copy`, one direction, `--delete`, excluding `.git/`, `node_modules/`, `.env*`, `dist/`, `.vercel/`, `.DS_Store`, `supabase/.temp/`. The manager runs it at the start of every job, so the copy always matches the owner's latest work **including uncommitted changes**.

Consequences, all intentional:
- No `.git` in the copy ⇒ **nothing there can ever push**, and there is no second repo to keep in sync.
- No `.env.local` ⇒ the (unused but real) `GEMINI_API_KEY` is not in the copy.
- OpenClaw admin mistakes cannot reach the real folder (see trap 3 — this is why the copy exists).
- **When OpenClaw eventually makes fixes**, they are carried back by diffing copy vs real and applying that patch in the owner's session — never by hand-copying files. Today this cannot happen: workers are read-only.

---

## 6. The fences, and the test that proves each

Run these again after ANY change to models, agents or policy. They are cheap.

| Fence | How it works | Proven by |
|---|---|---|
| No live connections in Claude workers | `env.vars.ENABLE_CLAUDEAI_MCP_SERVERS="false"` | A claude-cli turn listed exactly one MCP server, `openclaw` — no Supabase, Meta ads, Drive, Tally, Canva |
| Only approved programs run | exec `cautious` preset: allowlist + ask on-miss + **askFallback deny** | Worker: `head`/`ls`/`grep` WORKED; `touch`, `curl`, `git push --dry-run` all BLOCKED ("OpenClaw approval was not granted for native tool use (Bash)") |
| No file writing **BY WORKERS** (the manager writes reports, by design) | `tools.deny` + native Write is a prompt-gated tool | Worker's built-in Write BLOCKED; `/tmp` test files confirmed absent afterwards. **Re-proven unattended 2026-09-21:** a researcher asked to run `touch /tmp/openclaw-fence-test-2026-09-21.txt` raised an approval prompt, nobody answered, the tool call died at the 90s timeout and **no file was created**. Note the observed unattended behaviour is a **90-second tool timeout, not an instant deny** — same outcome, but it costs 90s, so a "stalled" job may just be a blocked command. — **and see the ⚠ below: this fence was NOT airtight until `sort`/`uniq` were removed** |

> ⚠ **`sort` and `uniq` are on the allowlist and BOTH CAN WRITE FILES. Found by the first real OpenClaw job, 2026-09-20, and confirmed by direct test.** `sort -o TARGET INPUT` writes `TARGET` **and overwrites it if it already exists**; `uniq INPUT TARGET` writes `TARGET`. Neither prompts, because both are allowlisted, so "read-only agents keep a shell restricted to look-only binaries" is **false as written** — two of the ten approved programs are not look-only.
> **What this actually allows:** overwriting or corrupting any file the user can write — including `~/Downloads/Website Flow Multi-Pickup` (so the copy no longer guarantees the real project is out of reach), `~/.openclaw/openclaw.json`, and launchd plists — plus copying any readable file to any writable path. **What it does NOT allow:** arbitrary *content*. Both programs can only emit a sorted/deduplicated permutation of lines that already exist in a readable file, and there is still no network, so this is corruption and local copying, not free-form authorship or exfiltration off the Mac.
> **CONFIRMED END-TO-END, THEN FIXED — 2026-09-20, with the owner's OK.** Proof, in this order (the order matters: closing the hole first would have made the test prove nothing):
> 1. **Before.** A real researcher agent, through the gateway, was asked to run `sort -o /tmp/openclaw-write-test-2026-09-20.txt CLAUDE.md`. It **succeeded, exit status 0, with no approval prompt**, producing a 21,589-byte file — byte-for-byte the size of the copy's `CLAUDE.md`. So this was never theoretical: a read-only worker could write.
> 2. **Fix.** `/usr/bin/sort` and `/usr/bin/uniq` removed from the `agents["*"]` allowlist, which is now **8 programs**: `chapter-check`, `chapter-refresh-copy`, `cat`, `head`, `tail`, `ls`, `wc`, `grep`.
> 3. **After.** Same command, same agent: **blocked** — `OpenClaw approval was not granted for native tool use (Bash)` — and no file created.
> 4. **No collateral damage.** `chapter-refresh-copy` still runs (645 files) and `chapter-check` is untouched; scripts invoke their own internals below the allowlist, which only gates the top-level command.
>
> **While doing this the allowlist table confirmed trap 7 out of its own mouth:** every entry reads **"any args"**. Arguments are genuinely unfiltered, so *any* approved program's write/exec-ish flags are live. **Before adding a program to this list, check its flags for a write path or a shell escape** — that is the real lesson, and it outlives these two binaries.
| No internet | `web_search`/`web_fetch`/`x_search` denied globally; browser plugin off | WebFetch blocked one layer earlier (ToolSearch denied) |
| No self-modification | `tools.deny` includes `gateway`, `plugins`, `secrets`, `automations`, `computer`, `terminal` | Config unchanged after the app relaunched; audit warnings dropped 5 → 3 |
| Agents can't read each other's work | `tools.sessions.visibility=tree`, `agentToAgent.allow` | Security audit stopped flagging cross-agent session access |
| Claude really is connected (not GPT pretending) | Claude Code's own records in `~/.claude/projects/...` | Nonce test: invented code `PROOF-030B50` appeared in Claude Code's own transcript, `entrypoint: sdk-cli`, model `claude-opus-5`, CLI **version 2.1.234** (the desktop app bundles 2.1.275, so it cannot be this session); second run caught the process live: `/Users/krutesh/.local/bin/claude` with parent pid = the gateway |

**`openclaw security audit`** (read-only) is the built-in check. Current: **0 critical, 3 warn**, all understood:
1. `gateway.trusted_proxies_missing` — not applicable, gateway is loopback-only.
2. `tools.exec.fs_tools_disabled_but_exec_enabled` — by design: read-only agents keep a shell restricted to look-only binaries. Closing it properly needs a sandbox. **OpenClaw's sandbox is DOCKER-based** (`tools.sandbox.mode`, plus `sandbox.workspaceAccess`; the schema carries `Sandbox Docker …` options throughout) and **Docker is not installed on this Mac** (checked 2026-09-20). It can therefore be closed **locally** — Docker Desktop + `sandbox` on + `workspaceAccess: ro` — and does **not** have to wait for a VPS.
   - **This is also the only honest way to give a worker internet access on this Mac.** Read is path-unrestricted here (trap 15), so an agent with both `cat` and the web can read `.env`, SSH keys or the gateway token and post them out — **read + internet is sufficient to leak; it does not also need write.** On a VPS that pair is harmless because the box holds nothing worth reading; on this Mac it is not. So: **no worker gets `web_search`/`web_fetch`/`browser` until the sandbox is on and the workspace is mounted read-only.** Until then the split "researcher browses / fixer writes" is prompt-only, and the audit is explicit that written "never" instructions are not a technical boundary.
   - **Not needed for Meta work.** The four Meta doc sets (~985 files) are already on disk, so a Conversions API / Pixel / Marketing API audit needs **no internet at all** — offline docs are also injection-free and rate-limit-free. Don't install Docker to do a job that doesn't need it.
3. `models.weak_tier` flagging `openai/gpt-6-astra` as "below GPT-5 family" — **false positive**, its table predates GPT-6.

---

## 7. Usage limits and resuming

- The two plans run out **independently**. Claude out → researcher/verifier/co-manager stop. ChatGPT out → the manager stops, so the whole job stops. **No cross-fallback is configured on purpose** (keeps spending predictable and the roles honest).
- OpenClaw retries briefly, then stops the step with the reset time, e.g. `You've reached your Codex subscription usage limit. Next reset in 7 minutes, Sep 20 at 4:15 AM`. Auth profiles go into a short cooldown. Nothing strange happens; the heartbeat lane fails cleanly.
- **There is no built-in auto-resume** — OpenClaw lists `usage_limited` as "reserved for a future usage-limit stop state". So the **heartbeat** does it: the manager runs a cheap turn whose prompt (`agents.defaults.heartbeat.prompt`) says: read the newest `*-progress.md`; if a job is unfinished and was stopped by a limit, refresh the copy and continue; if the limit is still in force or nothing is pending, reply "nothing to do"; never start new work.
- **Observed 2026-09-20:** a burst of setup tests exhausted the **5-hour** ChatGPT window while the **weekly** stayed at **84% left**. The 5-hour window is the binding constraint — pace bursts, and check `openclaw models status` first.
- **⚠ THE HEARTBEAT WAS QUIETLY EATING THE ChatGPT BUDGET — retuned 2026-09-20 (owner spotted it).** It ran **every 30 min on `gpt-6-astra`**, the flagship, to perform a trivial "is anything pending?" file check. The owner measured **~4–6% of the 5-hour window per run**: 10 runs per 5-hour window ⇒ **40–60% of the entire ChatGPT allowance consumed doing nothing**, on idle days. Fixed with three knobs (all hot-reload, no restart):
  - `agents.defaults.heartbeat.model` → **`openai/gpt-5.6-luna`** (the cheap tier; the heartbeat never needed a flagship)
  - `agents.defaults.heartbeat.every` → **`2h`** (was the built-in default ~30 min ⇒ 4× fewer runs; still far finer than the 5-hour limit it exists to wait out)
  - `agents.defaults.heartbeat.lightContext` → **`true`** (boolean, needs `--strict-json`)
  - **Other knobs available if it is still too expensive:** `activeHours` (skip quiet hours — but this also disables overnight resume, which is when it is most useful), `isolatedSession`, `timeoutSeconds`.
  - **The prompt was ALSO rewritten as a cheap fast path + expensive slow path (owner's idea, 2026-09-20).** The original prompt told the heartbeat to consider resuming and to follow AGENTS.md *before* it knew whether anything was pending, so it loaded context and deliberated on every check — including the ~95% that end in "nothing to do". It now reads **one** file (the newest `-progress.md`), and if nothing is outstanding replies `nothing to do` and **STOPs** — explicitly forbidden from opening AGENTS.md, planning or explaining. Only genuinely unfinished work triggers the full resume path.
  - **Why the heartbeat still runs as `main` rather than a cheap dedicated agent.** The owner correctly observed that detecting "is the goal finished?" needs almost no intelligence, and that the manager could work out *what* remains by itself — a detector/doer split. That design is right, but the **mechanism is undocumented**: `heartbeat.target`, `.session` and `.to` all have empty schema descriptions, and `main` is **not** in `agents.defaults.subagents.allowAgents`, so a cheap agent cannot simply spawn the manager. The resume path is what rescues interrupted overnight jobs, so it was not rebuilt on a guess. **If a day of measurement shows the heartbeat is still expensive, build the dedicated agent then — and test that it can actually wake `main` before trusting it.**
  - **⚠ THE REAL CAUSE WAS `isolatedSession`, AND IT WAS UNSET. Fixed 2026-09-20.** OpenClaw's own docs: *"`isolatedSession: true` … avoids sending full conversation history (~100K tokens down to ~2–5K per run)."* Without it the heartbeat runs **inside the manager's main session and re-sends the entire history every time** — which is exactly why a one-file check cost 4–6% of the 5-hour window. This is a known, widely-reported bug: **GitHub issue #20011, "built-in heartbeat only runs in main session — causes inevitable context bloat with no escape hatch"**; community reports describe ~120K tokens per trivial check, ≈$0.75 a run, and one user burning **$18.75 in a single night** on ~25 idle checks, with session files reaching 10–15 MB. **Set `isolatedSession: true` on any new install before anything else.**
  - **It also fixes a risk introduced by changing the heartbeat model.** The docs warn of **context bleed**: a heartbeat on a smaller model "can leave it active for the next main-session turn, potentially causing overflow." Isolation is the documented remedy, so the Luna switch is only safe *with* `isolatedSession: true`.
  - **Final heartbeat config: `model openai/gpt-5.6-luna · every 2h · lightContext true · isolatedSession true` + a fast-path prompt.**
  - **Deliberately NOT applied, with reasons — do not "optimise" these later without re-reading this:**
    - **`bootstrapMaxChars` 20000 → 10000** (the most-repeated community tip) **WOULD BREAK THIS INSTALL.** `AGENTS.md` is **12,093 chars** and the chapter அ section is at its **end**, so halving the cap silently truncates the team table, spawn models, golden rules and worker boilerplate. Total bootstrap is 24,578 chars against a 60,000 cap, so nothing is truncated today. **Re-measure before touching either number.**
    - **`activeHours`** (e.g. 09:00–19:00) saves idle-hours cost but **disables overnight resume**, which is the heartbeat's entire purpose.
    - **`target: "none"`** runs for internal state only and **does not deliver**, so the owner would stop being told when a job finishes or needs his decision.
    - **`HEARTBEAT.md` is obsolete** — migrated to monitor scratch via `openclaw doctor --fix`; `lightContext` does not need it. Don't create one.
  - **Still available if it is ever needed:** `/compact` or `/clear` on a bloated session (community reports ~47% cost cut), `contextPruning` in cache-ttl mode, `agents.entries.*.params.cacheRetention`, and `/context list` for a per-file token breakdown. The main session sat at **40k/258k tokens with a ~7 MB store** on 2026-09-20 — worth compacting before a big job, but harmless.
  - **Do NOT "fix" this by moving the heartbeat onto a Claude model.** Claude Pro is the *scarcer* pool — it is shared by the researcher, verifier, co-manager **and the owner's own Claude Code sessions**. Moving the drain there makes things worse, not better.
  - **Unverified:** whether `gpt-5.6-luna` draws from the *same* 5-hour pool as `gpt-6-astra` or a separate one. `openclaw models status` reports a single pooled `openai usage` figure, so a cheaper model probably weighs less against the same pool rather than escaping it. Confirm by watching the drop per heartbeat over a day rather than assuming.
- The Mac must stay awake and online: plugged in, and sleep prevented. A closed lid stops everything.

---

## 7a. The resume mechanism was REPLACED on 2026-09-20 evening (ChatGPT session)

**The heartbeat no longer polls.** `agents.defaults.heartbeat` is now `every: 0m` with `lightContext`/`isolatedSession` still true, and the `model` (Luna) and fast-path `prompt` overrides **removed** — so any remaining event-driven wake runs on the normal Astra manager. In its place is a scheduled **model-free gate**.

**Why this is the right direction, and should be kept.** The argument that won: *the tuned heartbeat still used AI to decide whether to use AI*, and it combined detection with execution in one prompt. Those are different jobs. A code gate that makes **zero model calls while idle** can therefore run every 5 minutes — more responsive than the 2-hour poll *and* cheaper. Full research (well sourced, with the limits of each source stated) is preserved at **`~/.openclaw/workspace/reports/2026-09-20-heartbeat-research-chatgpt.md`** — the original was in `/private/tmp`, which is wiped on reboot.

**What is installed:** automation `chapter-auto-resume` (id `48360f33-a5f2-4039-ac1f-9214b6941bb5`), `every 5m + trigger`, `agentId main`, `sessionTarget isolated`, `delivery none`, payload model `openai/gpt-6-astra`, `thinking medium`, `timeoutSeconds 14400`, `toolsAllow ["*"]`. State lives in `~/.openclaw/workspace/resume/resume-state.json` with a contract at `resume-state-contract.md`. The gate fails closed: it fires only for an explicitly authorized job with a valid checkpoint inside the reports folder, and records a checkpoint fingerprint so unchanged work is not relaunched.

**⚠ What was NOT finished — do not assume this is proven:**
- The session **stopped mid-Phase-7** when its ChatGPT allowance ran out, while investigating what it believed was a fence regression (it was not one — trap 21).
- Its own acceptance list is only partly tested. Verified with a fake payload: idle suppresses, an authorized checkpoint fires once, the next tick records `already-fired`. **Not verified: a real Astra handoff, duplicate prevention across a gateway restart, Mac sleep/wake recovery, checkpoint-vs-source consistency, and the `no-route` delivery failure it found in the old heartbeat receipts.**
- **`toolsAllow: ["*"]` with a 4-hour timeout on a job that evaluates every 5 minutes** is a broad blast radius if the gate ever misfires. The gate is what keeps it shut, so the gate deserves real testing before this is trusted unattended.
- **Resolved later:** the recurring jobs were inventoried. After `researcher2` was added there are five Skill Workshop review definitions; all are disabled. The owner then disabled proposal capture and Memory Dreaming as well, so no autonomous learning model runs remain. See to-do 14.

**Housekeeping from that session:** it wrote two approvals dumps (`openclaw-approvals-before-worker-lockdown.json`, `openclaw-approvals-worker-lockdown.json`) into the **real project root**, where an untracked file can be swept into a commit. They contain **no secrets** — both record the same 8-program allowlist and the same `allowlist/on-miss/deny` defaults, confirming the "worker lockdown" changed only per-agent scoping — and were moved to `~/.openclaw/backups/`. **Config backups belong under `~/.openclaw/`, never in the git repo.**

**Single-provider-limited rules were added to AGENTS.md on 2026-09-21, because the original assumed both providers were up.** The allowances are separate — **ChatGPT** runs the manager + Researcher A, **Claude** runs Researcher B + verifier + co-manager — and the failure is **asymmetric**:
- **Claude out, ChatGPT free:** the job no longer stalls. For a **small, tightly-scoped** job the manager plans alone (stamping **"PLAN NOT INDEPENDENTLY REVIEWED"** on the report), researches on ChatGPT, records results under **"UNVERIFIED CANDIDATES — awaiting verifier"**, and sets `waiting_for_limit` / `blockedProvider: anthropic`. The gate resumes it after Claude's reset, runs the verifier first, *then* labels. **A check is deferred, never skipped — nothing is reported as confirmed that the verifier has not seen.** For a **broad** job it still waits, because a wrong plan wastes the whole budget and that review is what prevents it.
- **ChatGPT out:** everything stops. No partial progress is possible — the manager *is* the orchestrator, and without it there is nobody to delegate to. **Claude running out costs verification; ChatGPT running out costs everything.**
- Observed live on 2026-09-21: ChatGPT **100% left** (just reset) while Claude sat at **95% used** — the exact case the old rules did not cover.

**A budget interaction worth remembering:** the researcher was moved to `openai/gpt-5.6-luna` that afternoon, and this ChatGPT session then **exhausted the ChatGPT allowance**, which left the researcher unable to run at all until the reset while Claude still had capacity. Concentrating roles on one provider removes a bottleneck and creates a single point of failure at the same time.

---

## 7b. Notifications — what the bot will and will not tell the owner (built 2026-09-20)

**Before this, OpenClaw could not tell the owner anything**: no channel existed, delivery logged `no-route`, and reports landed silently on disk. `message` is globally denied (and stays denied), so notifications travel by **scheduler delivery**, never by an agent deciding to message someone.

| # | The owner IS messaged when… | Mechanism |
|---|---|---|
| 1 | He messages the bot first | Telegram channel, `dmPolicy: allowlist` |
| 2 | The resume gate fires and the manager run finishes | `chapter-auto-resume` · `delivery: announce → telegram` |
| 3 | **Any new FINAL report appears in `reports/`** | `report-ready-notify` (`604302ab…`), every 5m |
| 4 | **The resume gate errors twice in a row** | `chapter-auto-resume` `failureAlert` after 2, 1h cooldown |
| ~~5~~ | ~~The resume gate stops ticking~~ | **NOT BUILT — DISABLED. See below.** |

**✅ DELIVERY PROVEN 2026-09-21 05:40 IST — the first message this bot has ever sent.** With the owner's OK, a real file `2026-09-21-delivery-test.md` was dropped into `reports/`. Within one 5-minute tick the watcher went `no-change` → **`new-report`**, `lastRunStatus` **`error` → `ok`**, `lastDeliveryStatus` **`unknown` → `delivered`**, `lastDeliveryError` cleared, and the dashboard's red ✗ turned green. Evaluation count 69 → 70. **Leave that test file in place:** the trigger compares `newest !== lastSeenReport` — *different*, not *newer* — so deleting it makes an older report become newest and fires a second, misleading announcement. That inequality is also a live quirk worth knowing: **re-writing an existing report fires the watcher again**, because `ls -t` is modification-time ordered.

**⚠ A HYPOTHESIS THAT WAS WRONG, recorded because the reasoning looked sound and someone will have it again.** Before the test this session predicted delivery would FAIL, because the payload is `sh -lc "ls … | grep … | head -1 | sed …"` and **neither `sh` nor `sed` is on the 8-program exec allowlist** — while the *trigger* script's `/bin/ls` visibly goes through that gate (`openclaw approvals get` showed `/bin/ls` "last used 1m ago" from this very watcher). The prediction was wrong: it ran fine. **The rule that actually holds is narrower than §7b implied — a cron TRIGGER script is bound by the exec allowlist; a cron `--command` PAYLOAD is not.** Two consequences: watcher payloads may freely use pipes, `sed`, `awk` and anything else, so do not "fix" them to fit the allowlist; and the scheduler is a **wider blast radius than the agent fence**, which sharpens the existing §7a concern about `toolsAllow: ["*"]` on the resume gate — the gate's own condition script is the only thing holding that shut.

**Follow-up, same session — the question "is that a security hole?" answered with evidence, not reassurance.** It is not, and the reason is worth keeping:
- **No agent can create or edit an automation.** Global `tools.deny` carries `automations`, `gateway`, `plugins`, `secrets`, `terminal`, `computer`, `message`, `browser`, `web_search`, `web_fetch`, `x_search`. So the "payloads escape the exec allowlist" finding is **not agent-reachable** — exploiting it needs something that can already write config, i.e. the owner or a Claude session.
- **`toolsAllow: ["*"]` on the resume gate is narrower than it looks.** OpenClaw's documented filtering order is explicit that **deny wins absolutely** and *"each level can further restrict tools, but cannot grant back denied tools from earlier levels."* So that `*` cannot re-enable `automations`, `secrets`, `gateway`, `message` or the web tools — it only widens within what was already permitted. **Do not read `["*"]` as "all tools".** (Docs: `gateway/config-tools/built-in-tools`, `tools/subagents/tool-policy`.)
- **What remains true, and is the honest risk, is unchanged and already recorded:** read is path-unrestricted (trap 15), so any agent with `cat` can read `~/.openclaw/openclaw.json`, `.env*` or SSH keys — it just cannot write or send them. That is why **read + internet is the one pair that must never be combined on this Mac**, and why the split-model idea and the VPS/Docker plan are the same project. The security audit states the trust model outright: *"personal assistant (one trusted operator boundary), not hostile multi-tenant."*

**⚠ SCRATCH FILES BESIDE THE REPORT SET OFF FALSE "REPORT READY" ALERTS — found and fixed 2026-09-21.** The watcher is a **blocklist**: it announces any new `.md` in `reports/` that does not end in `-progress`, `-working`, `-verification` or `-manager-position`. The `capi-full` run invented four new suffixes — `-a1-map`, `-a3-candidates`, `-b-headings`, `-b-manager-draft` — and **each one told the owner a report was ready when it was not**, four false alerts before the real one. **Fix applied:** `AGENTS.md` now says intermediates go in `~/.openclaw/workspace/reports/working/`; only the final report, `-progress` and `-working` belong directly in `reports/`. This run's four strays were moved there. **The deeper defect is unfixed:** a blocklist cannot anticipate a suffix a future job invents. The robust version is a *subfolder* convention (done) or an allowlist in the trigger script (not done — it means editing a live cron's condition script, and nothing was running, so it was deferred rather than risked mid-session). If false alerts recur, that is the change to make.

**Latency, measured 2026-09-21 on the real job:** report written 05:55:57, the watcher's previous check was 05:55:34 (**23 seconds too early**), it fired at the next tick 06:00:52 and delivered. **So expect up to ~5 minutes between a report landing and the phone buzzing.** That is the 5-minute poll working, not a fault — worth knowing before anyone debugs a "missing" notification that is merely early.

**All of 3/4/5 cost ZERO model calls.** Each is a condition script plus a `--command` (shell) payload; no `agentTurn` is involved, so they are free while idle and free when they fire.

- **`--failure-alert-exclude-skipped` is mandatory on the gate.** It legitimately reports `skipped` on every idle tick; counting skips as failures would alert every five minutes forever.
- **`bestEffort: true` on every delivery** so a Telegram outage can never kill a job.
- **`report-ready-notify` baselines on its first run** — it records the newest existing report and only fires on something *newer*, so enabling it does not announce the whole back catalogue. It ignores `-progress`, `-working`, `-verification` and `-manager-position` files.
- **`gate-health-check` re-alerts at most hourly** and reads the gate's `lastCheckedAt` straight from `cron_jobs.state_json` via `sqlite3 "file:…?mode=ro"`.
- **Scripts + their test harnesses live in `~/.openclaw/workspace/watchers/`.** Logic was proven before deployment — report watcher **6/6**, health checker **8/8** (harnesses in this session's scratch; the gate's own is at `~/.openclaw/workspace/resume/gate-harness.mjs`). **Re-run the harness pattern after editing any condition script.**

**⚠ #5 (silent-stall detection) COULD NOT BE BUILT, and the attempt is disabled (`bde3cb4a…`).** The checker needed to read the gate's `lastCheckedAt`, which lives **only** in `cron_jobs.state_json` inside SQLite. **Condition scripts ARE bound by the exec allowlist** — the gate's own script works solely because `/bin/cat` is on it — and `sqlite3` is not. It was deliberately **not** added: `sqlite3` can write databases and has shell escapes, i.e. exactly the class of hole closed by removing `sort`/`uniq` (see the ⚠ in §6). A filesystem search confirmed **no `cat`-readable file carries a per-tick timestamp**, and nothing inside the fence can write one, since no allowlisted program writes. So within the current fence this is **not buildable**.
- **The first attempt failed silently in the worst way**: `decision=checker-error` every 10 minutes from a catch-all handler, while `errors=0` and the job looked healthy. It was disabled rather than left in place — **a broken watcher is worse than no watcher, because it manufactures false assurance.** Scripts kept at `~/.openclaw/workspace/watchers/gate-health-gate.js` + `gate-stale-alert.sh` for whoever fixes it.
- **What DOES cover tonight's actual failure: #4, the failure alert** (`after: 2`). The 2026-09-20 outage was the gate *erroring* (trap 23), and that alert would have caught it. The uncovered case is narrower: a gate that stops ticking **without** recording errors.
- **The proper fix is OUTSIDE OpenClaw** — a macOS `launchd` job is not bound by the exec allowlist and can read the DB and call the Telegram bot API directly. That is also the only honest design: **nothing inside OpenClaw can reliably detect OpenClaw being dead.** It needs the bot token, so the owner must wire that part.

**STILL NOT covered — know these gaps:**
- **A job the owner starts by hand in the dashboard that finishes normally** is only caught by #3, i.e. when it *writes a final report*. A job that finishes without writing one is silent.
- **A job that pauses on a usage limit** does not notify — the manager writes `waiting_for_limit` to the state file and stops. #5 will not fire either, because the gate is healthy and correctly waiting.
- **If the whole scheduler dies, all three watchers die with it.** They catch one job failing, not the gateway failing. Nothing on this Mac watches the watchers.

---

## 8. Traps already hit — do not repeat these

1. **Never restart the gateway / install plugins / change config while the Mac app's onboarding is open.** Doing so broke the app's sign-in step ("The Gateway did not complete activation… retry the saved sign-in"; log: `config reload failed: The plugin or restart requirement changed before activation`). Fix was to quit the app, let it settle, and clear the stale `openclaw.onboardingSystemAgentPending` defaults key; the relaunch then logged `openclaw.setup.verify ✓`.
2. **Redaction: BSD `sed` has no `\s`.** A redaction regex silently failed and printed the gateway auth token into the chat transcript. Mask with `sed -E 's/[A-Fa-f0-9]{32,}/<hidden>/g'` (and for base64-ish, `[A-Za-z0-9_+/=-]{40,}`). If a token does leak: `openclaw config set gateway.auth.token "$(openssl rand -hex 24)"` then `openclaw gateway restart` (done once already; fingerprint ecb26dd5 → 6c6b1682).
3. **`openclaw agents delete <id>` moves that agent's WORKSPACE to the Trash.** The retired `worker` agent's workspace was the owner's real project folder. Safe procedure: first `openclaw config set agents.entries.<id>.workspace <an empty dir>`, then `openclaw agents delete <id> --force --json`, then confirm the project file count and `git rev-parse --short HEAD` are unchanged (they were: 6748 files, caa2689). **This near-miss is why OpenClaw now works on a copy.**
4. **Sub-agent model precedence.** A cross-agent spawn used `agents.defaults.subagents.model` and ignored the target agent's own `model`, so the "Opus verifier" silently ran on Sonnet (the manager itself reported the mismatch). Fix: set per-agent `agents.entries.<id>.subagents.model` **and** tell the manager in `AGENTS.md` to pass `model` explicitly. **Always verify from the log line** `cli turn: provider=claude-cli model=…` — never from the config alone.
5. **claude-cli native tools are all-or-nothing.** In this OpenClaw version `resolveCliNativeToolApprovalPlan` returns *allow* only for `security=full, ask=off`, *deny* for `security=deny`, and otherwise **prompt** — for every native tool including **Read**. Unattended, that means denied. Native **Bash** is the exception: under `ask: on-miss` it is checked against the exec allowlist. **Consequence: give Claude agents read access through allowlisted look-only binaries (`cat`, `head`, `grep`…), never by loosening exec.**
6. **Never set a claude-cli agent to exec `full`.** Tried once for the read-only worker: per-agent `tools.deny` did **not** restrict Claude's native tools, and the worker then ran shell, wrote `/tmp`, and fetched the web. Reverted within a minute; nothing harmful ran. The audit's own advice for read-only agents is deny `exec`+`process`, or a real sandbox.
7. **The exec allowlist approves whole programs, not arguments** (arg-level pinning is an exact-argv hash). That is why helper wrappers exist instead of allowlisting `git`.
8. **`openclaw agent -m "…"` returns EMPTY output when the manager hands off and yields.** That is not a failure. Read `openclaw sessions list` and `openclaw logs --limit N`.
9. **Config types are strict:** `env.vars.X` must be a **string** (`'"false"' --strict-json`), booleans/numbers need `--strict-json`, and object merges need `--merge`.
10. **Batch config changes.** After 3 CLI restarts in 10 minutes the gateway warns about a "restart storm".
11. **`openclaw sessions delete` needs `--yes`**; `openclaw config unset` refuses to drop an agent entry (use `agents delete`, with trap 3's precautions).
12. **zsh:** `$var:word` triggers history modifiers — always write `${var}` in loops (this mangled a session key mid-test).
13. **ChatGPT models need the official `clawhub:@openclaw/codex` plugin.** Without it the app says "No agent model is configured" even though the sign-in worked. Install official-publisher plugins only.
14. **Don't use models you can't vouch for.** `claude-cli/claude-fable-5-1`, `claude-fable-5`, `claude-mythos-5`, `openai/gpt-5.6-sol|terra|luna` are in the catalog but were deliberately not used.
15. **Read access is path-unrestricted on the Mac.** `cat` is allowlisted, so an agent can read any file the user can (including `~/.openclaw/openclaw.json`). They cannot write or send anything, and the copy protects the real project, but only a VPS or a Docker sandbox gives true isolation.
16. **Never ask an agent "what folder are you in?" — `pwd` is not allowlisted, so the question cannot be answered.** This stalled to-do 1's own test on 2026-09-20: the researcher asked to run `pwd`, sat as a pending approval for ~3 minutes, and under `askFallback: deny` an unattended run would simply have lost that time. **Prove location by property, not by path string:** `ls -a` (allowlisted) shows the copy has **no `.git`** while the real project does, which proves both *where the agent is* and *that it cannot reach git* — strictly more than `pwd` would have proven. A denied approval costs real time but is not fatal: the researcher recovered by itself, though the turn took **185s instead of ~29s**.
17. **`openclaw logs --max-bytes` caps at 1000000, and going over returns NOTHING, not an error.** `--max-bytes 2000000` fails server-side as `INVALID_REQUEST` while the shell pipeline still exits 0, so a `grep` over it prints zero lines and looks exactly like "no matching events". Same silent-pass trap as `CLAUDE.md`'s SQL-assertion rule. Keep `--max-bytes` ≤ 900000, and if a log query returns nothing unexpectedly, re-run it without the grep before believing the silence.
18. **Long worker replies reach the manager TRUNCATED, and the manager cannot tell what it lost.** On the first real job (2026-09-20) **all four** researcher returns plus the co-manager's decision-point-1 answer came back cut off; the manager flagged each one and refused to treat unread text as reviewed, which is the right behaviour but means evidence is silently dropped. There is no retention/truncation key in `openclaw.json` to raise — it is internal. **Mitigation: cap what workers return, not what they read.** Tell every worker task to reply with at most N findings, each at most ~3 lines, in a fixed shape (claim · doc file + quote · `file:line` · one-line reasoning), and to write nothing else. A worker that answers in long prose will lose its best evidence off the end. Treat a manager note of "truncated" as a reason to re-ask narrowly, never as a reason to drop the area.
19. **Memory recall is degraded: embedding credits are exhausted.** The manager reported on 2026-09-20 that recall fell back to keyword search, and that the suggested repair (`openclaw memory status --index --agent main`) **may incur provider costs** — so it was not run. Jobs still work; only semantic recall of past sessions is weaker. Decide with the owner before spending on re-indexing.
20. **`grep -rl "<name>" <dir>/` OVER-REPORTS — it matches the name inside comments, not just real imports.** `CLAUDE.md` tells you to re-derive the metaCapi importer list with exactly that command; run it and you get **six** files, but `meta-signal-watchdog/index.ts` only *mentions* the filename in three comments. CLAUDE.md's "five importers" is correct. The assistant hit this on 2026-09-20 and briefly announced a false finding before checking. **Always open the matched line and confirm it is an `import`, not prose** — and note that the false positive is harmless for redeploys (you'd redeploy one extra function) but poisonous in a written finding. This is the single best worked example of why the verifier role exists.
21. **A fence test you APPROVE proves nothing — and it is easy to scare yourself.** On 2026-09-20 a ChatGPT session ran a worker fence check, reported *"the researcher could run `/usr/bin/touch`, and the output confirms it actually wrote the test file"*, and treated it as a live security regression. **It was not.** Re-tested immediately afterwards with nobody approving: the worker got `Exec allowlist miss: touch …`, the run returned `blocked: OpenClaw approval was not granted for native tool use (Bash)`, and **no file was created**. The allowlist was unchanged (8 programs, no `touch`) and the effective policy still read `ask: on-miss` + `askFallback: deny`. The overwhelmingly likely explanation is that the tester **granted its own approval prompt** and then read the resulting file as a bypass. **Rule: a fence test is only valid if the approval is DENIED or left to expire. If you clicked allow, you tested the human, not the fence.**
23. **ENABLING, DISABLING, OR RECONFIGURING A PLUGIN CAN BREAK IN-FLIGHT CRON TRIGGER EVALUATION UNTIL THE GATEWAY IS RESTARTED.** On 2026-09-20 the telegram plugin was enabled while `chapter-auto-resume` was running. Every tick afterwards failed with the receipt `cron trigger evaluation failed (internal_error): Plugin inventory has retired; begin a new plugin operation.` The scheduler applied error backoff, and the 5-minute gate silently stretched to 20 minutes with **no alert of any kind** — the job still read `enabled: true` throughout. `openclaw gateway restart` cleared it: `consecutiveErrors` 3 → 0 and the next tick succeeded (evals 38 → 39, decision `idle`). The same failure recurred on 2026-09-21 after changing the Memory Dreaming plugin configuration: evaluations 68–69 failed, then a gateway restart produced healthy evaluation 70 (`idle`, `consecutiveErrors: 0`); `report-ready-notify` recovered too. **After ANY plugin enable/disable or plugin-backed config change, restart the gateway and confirm a cron trigger actually ticks** — read `triggerEvalCount` and `lastCheckedAt` from `openclaw automations get <id> --json`, twice, a few minutes apart. **The counter moving is the only proof a gate is alive; "enabled: true" is not.**
24. **NEVER write config while the owner is running `openclaw configure` — the wizard's write is silently lost.** On 2026-09-20 `channels.telegram.dmPolicy` and `.groupPolicy` were set from this session at 23:15:26/30 while the owner pasted a Telegram bot token into the wizard. The log recorded `config reload superseded: GatewayConfigReloadSupersededError: config reload superseded by a newer runtime config source`, and the token **never landed** — `channels.telegram` ended up holding only the two policy keys. The `diagnostic_events` table's `config-audit` rows show all four writes in that window were the assistant's; the wizard's was not there. **There is no error shown to the person running the wizard; it reports success.** Rule: when the owner is entering credentials, stop touching config entirely, wait for them to confirm, then verify read-only.
25. **THE DASHBOARD'S "LAST RUN" IS NOT GATE HEALTH — a healthy gate can show a red ✗ forever.** `lastRunStatus` is sticky and records the last *actual execution*; a condition that correctly declines to fire records **`skipped`**, which never overwrites it. So after the 2026-09-20 plugin-inventory incident (trap 23) the dashboard kept showing **⚠ / ✗ "48m ago"** while the gate was demonstrably fine — `triggerEvalCount` climbing, `consecutiveErrors: 0`, and six consecutive `skipped` receipts exactly 5 minutes apart. **Until a gate fires for the first time, that row will display its last error indefinitely.** Judge health only by **`triggerEvalCount` moving** and **`lastCheckedAt` being recent**, from `openclaw automations get <id> --json`. This is also why `--failure-alert-exclude-skipped` is mandatory: without it every healthy idle tick counts as a failure.
22. **`tools.exec.mode` is INTERSECTED with the host policy, not simply applied — a looser per-agent request is silently overridden.** The same session set the three workers from `mode: "ask"` to `mode: "allowlist"` (which requests *never ask*). `openclaw approvals get` shows what actually happened: `mode requested=allowlist effective=ask · ask requested=off effective=on-miss · note="more aggressive ask wins"`. So the change had **no weakening effect** — but equally, **you cannot loosen exec from the agent entry, and reading `openclaw.json` alone would have told you the opposite.** Always read `effectivePolicy` from `openclaw approvals get`, never the raw config, when judging what exec will actually do.

---

## 9. TO-DO (in order)

1. **Run the end-to-end team test that was cut short by the usage limit.** Check the budget first (`openclaw models status`). Command:
   ```bash
   ~/.openclaw/bin/openclaw agent --agent main --session-key "agent:main:setup-test" -m 'SETUP TEST (small, read-only). Follow AGENTS.md exactly. 1) Run chapter-refresh-copy. 2) Spawn a researcher (agentId "researcher", model "anthropic/claude-sonnet-5") to run `ls -a` (no path argument) and `head -1 CLAUDE.md`, then report (a) whether a `.git` entry is present and (b) the first heading line of CLAUDE.md. **Do NOT ask for its "current working folder"** — that drives it to `pwd`, which is not allowlisted (trap 16). 3) Write your own one-line position: is the copy correctly wired for audits? 4) Ask the co-manager (agentId "comanager", model "anthropic/claude-opus-5") for an INDEPENDENT position on the same question, giving it only the researcher evidence, never your own view. 5) Reply in 5 short lines: researcher folder, researcher heading, your position, co-manager position, AGREE or DISAGREE.'
   ```
   **Pass =** researcher's folder is `~/OpenClaw-Workspace/chapter-copy`, the heading is `# chapter அ — Claude Code guide`, and the log shows both `model=claude-sonnet-5` and `model=claude-opus-5`. This also confirms `tools.sessions.visibility=tree` did not break cross-agent spawning — **if spawning fails, that is the first suspect**; fall back to `visibility=all` plus the `agentToAgent.allow` list. Delete the test session afterwards (`sessions delete … --yes`).

   **DONE 2026-09-20 — PASSED.** The whole chain ran: `chapter-refresh-copy` → `sessions_spawn` researcher (`model=claude-sonnet-5`) → manager yielded → manager resumed → `sessions_spawn` co-manager (`model=claude-opus-5`) → 5-line reply. **`visibility=tree` does NOT break cross-agent spawning** — that suspicion is now cleared, no fallback needed. Test sessions deleted.
   - **Two things the test got right that matter more than the pass itself.** (a) The manager ran `chapter-refresh-copy` first and the copy verified clean afterwards: 645 files, mtimes identical to the real project, **no `.git` and no `.env*`**. (b) When the researcher could not verify its folder, **both managers refused to claim it was verified** — manager: "audit wiring remains unconfirmed"; co-manager: "readable guide alone does not confirm the working folder"; verdict AGREE. That is exactly the anti-rubber-stamping behaviour the verifier bake-off exists to select for, and here it appeared unprompted.
   - **The prompt above is still the one that was run, and it contains a trap — fix it before re-running.** Asking for "its current working folder" drives the agent to `pwd`, which is deliberately not allowlisted (trap 16), so that line can never be answered and the run stalls ~3 min on a pending approval. Replace step 2 with: *run `ls -a` (no path argument) and `head -1 CLAUDE.md`, then report whether a `.git` entry is present, the heading line, and the first 10 entries.* Re-verified this way on 2026-09-20: `.git` **NO**, heading `# chapter அ — Claude Code guide`, listing `., .., .agents, .claude, .codex-qa, .githooks, .github, .gitignore, .idea, AFFILIATE-LINKS-HANDOFF.md` — which proves the researcher is in the copy **and** cannot reach git.
2. **Verifier bake-off: Opus 4.7 vs Opus 4.8** (owner's request). **BLOCKED — re-checked 2026-09-20 (second time that day): `claude-opus-4-8` is still absent from the catalog, so 4.7 stays and no result was invented. Nothing to do here until 4.8 ships.** **Note: `claude-opus-4-8` was NOT in the catalog on 2026-09-20** — only `claude-cli/claude-opus-4-7`, `claude-cli/claude-opus-4-6`, `claude-cli/claude-opus-5`. Re-check with `openclaw models list | grep -i opus`; if 4.8 is absent, say so and keep 4.7 rather than inventing a result. Method that was used for Sonnet 5 vs Opus 4.7 — reuse it exactly, same 6 claims, same prompt, one run per model, score out of 6:
   - (1) `_shared/metaCapi.ts` sets the Graph API version to v26.0 — **TRUE** (line 60)
   - (2) `meta-catalog-feed/index.ts` imports `_shared/metaCapi.ts` — **FALSE** (it imports only supabase-js)
   - (3) `CLAUDE.md` says the unique key is `applications(event_slug, phone)` — **TRUE**
   - (4) `CLAUDE.md` says `events.payment_mode` can be split, full **or installment** — **FALSE** (only split | full)
   - (5) `src/bookingTimeline.ts` exports `stepRole` — **TRUE** (line 82)
   - (6) `CLAUDE.md` says the `meta-signal-watchdog` cron runs **every hour** — **FALSE** (daily)
   Run as `--agent verifier --model <model> --session-key agent:verifier:bake-<tag>`. **Result on 2026-09-20: Sonnet 5 = 5/6 (it CONFIRMED the planted "installment" claim while quoting the line that disproves it — rubber-stamping), Opus 4.7 = 6/6.** Keep whichever catches more planted errors; update `agents.entries.verifier.model`, its `subagents.model`, its `name`, and the model line in `AGENTS.md`.
3. **First real job:** both managers review the OpenClaw safety setup itself, then the Conversions API audit. Read-only; report to `~/.openclaw/workspace/reports/`.
   **HALF DONE 2026-09-20 — the safety review ran and passed; the Conversions API audit is still to do.** Report: `~/.openclaw/workspace/reports/2026-09-20-openclaw-safety-review.md` (19 KB), plus `-progress`, `-working`, `-verification` and `-manager-position` files beside it. Evidence bundle: `~/.openclaw/workspace/reports/evidence-2026-09-20/`. Cost roughly **one third of the 5-hour ChatGPT window and 5% of the week** — budget the CAPI audit accordingly.
   - **Overall verdict: PARTIALLY COMPLIANT** — *"the supplied live-configuration snapshots match important named restrictions, but do not establish the broad isolation promised by the handoff."* All ten verdicts agreed by both managers; no unresolved disagreement. No compromise, exploit, current token leak or production access was demonstrated — and the report says plainly that absence of such proof is not proof of safety.
   - **The one fence that is actually broken is `sort`/`uniq`** (the ⚠ in §6). The job could only label it UNCERTAIN because it is forbidden from running tests; this session confirmed it by direct test.
   - **"No file writing" is NON-COMPLIANT as written** — the *manager* can write, by design, because that is how reports get saved. This is a wording defect in §6, not a hole: the fence was always about workers. Fix the sentence, do not remove the manager's ability to write.
   - **Give the evidence, not the live files.** The review read a **redacted snapshot** (`openclaw config` output has to come from the raw file; mask with the §8 trap 2 regexes and verify no long hex/base64 survives). Never point a job at `~/.openclaw/openclaw.json` itself — it holds the gateway token. Side effect worth knowing: redaction also means the job **cannot verify token rotation**, and it correctly refused to claim it either way.
   - **Structural lesson worth more than any single finding: a read-only auditor cannot close findings that need execution.** Expect every audit to hand back a short "UNCERTAIN — needs a test" list. That is the system working. Closing them is a job for an interactive session with a shell, or the owner.
   - **Five owner decisions are listed at the end of the report** (broad-read acceptability, manager write capability, whether to authorise a bounded proof test of worker writes, whether the historical token-rotation account is sufficient, and what evidence would make a future VPS acceptable). They are questions, not blockers.
4. **Back up the owner's project.** It exists **only on this Mac**, with a couple of dozen commits not on GitHub (**re-count with `git log origin/main..HEAD --oneline | wc -l` — it was 27 early on 2026-09-20 and 28 by that evening, because the owner commits from his own sessions too**) plus uncommitted files. The copy is not a backup. This is a 10-minute job and is overdue.
5. **Move OpenClaw to a VPS — ON HOLD by the owner's decision, 2026-09-20. Do not re-open this or re-research hosting until he says so.** His reasoning, and it is sound: OpenClaw's *value is still unproven* — it has been set up and audited but has barely done any real work — so buying infrastructure for it now is premature. The plan stays: run it locally, keep it read-only, and revisit paying for a server only once it has demonstrably earned it. **The next step that matters is therefore to-do 3's second half (the Conversions API audit), not hosting.**
   - **Oracle attempt, 2026-09-20 — blocked on capacity, everything else worked.** Account is live (tenancy `krutesh08`, region `ap-hyderabad-1`, Free Trial). A VCN `openclaw-vcn` with a public subnet was created, and an instance configured as **Ubuntu 24.04 Minimal aarch64 + VM.Standard.A1.Flex, 2 OCPU / 12 GB**, public IPv4 on, SSH key attached — then Create failed: *"Out of capacity for shape VM.Standard.A1.Flex in availability domain AD-1."* Hyderabad has only **one** AD and the home region is permanent, so "try another AD" is not available. **The config was saved as a Resource Manager stack — re-running that stack is a one-click retry, far better than redoing the wizard.**
   - **Things that cost time and must not be re-learned:** (a) **Create the VCN BEFORE the instance** — build it inside the instance wizard and the public-IP option greys out, leaving a server you cannot SSH into. (b) The **OCPU/memory fields are hidden behind a ▶ triangle** on the shape row; miss it and you silently get 1 OCPU / 6 GB. (c) **SSH keys live in the Networking step**, not step 1. (d) Leave IPv6 **off** — Oracle ARM instances often get a non-routing IPv6 address and Node's dual-stack race then times out.
   - **Always Free was HALVED on 15 June 2026 to 2 OCPU / 12 GB** (was 4 OCPU / 24 GB). Do not quote the old figure.
   - **An SSH keypair already exists for this**: `~/.ssh/openclaw-vps` (ed25519, no passphrase, public half was uploaded to Oracle). Reuse it rather than generating another.
   - **Hosting research is DONE — do not redo it** (2026-09-20, for a 4 vCPU / 8 GB box). **Hetzner CX33 ~€8.49/mo (~₹850)** is the best value and x86 is now *cheaper* than their ARM CAX21 (€10.49) after the June 2026 adjustment — but Hetzner **frequently rejects Indian customers at KYC**, which is a real time risk. **Contabo ~₹450/mo** is cheapest (Germany, no UPI). **GigaNodes ₹1,800/mo** (Noida/Mumbai Yotta DC) takes **UPI**, bills monthly and **does not raise prices at renewal**. **Hostinger KVM 2** looks cheapest at ₹799 but demands **2 years upfront (~₹22,600)** and renews at ₹1,199 — bad shape for experimental work. **DigitalOcean, Vultr, Linode and AWS all refuse UPI and bill only in USD.** Latency is irrelevant here — OpenClaw calls remote APIs, it does not serve customers — so a European box is functionally equivalent.
   Original plan when it resumes — see the `oracle-vps-for-openclaw` memory. Always-Free ARM shape ≤ 2 OCPU / 12 GB in **Hyderabad** (his home region, fixed), Tailscale + SSH hardening, gateway stays on loopback, both subscription logins (ChatGPT via `--device-code`, Claude Code via a browser step through an SSH tunnel), copy the project + docs, **re-apply every fence in §4 and re-run every test in §6 there**, and add the Docker sandbox that closes audit warning 2.
6. **WhatsApp control**, when the owner gets a spare number: link via QR (WhatsApp Web/Baileys), `dmPolicy: "allowlist"`, `allowFrom` = only his own number. **Never his personal or business number** — his customer chats live there, and unofficial-client automation risks a ban. Approval prompts can then be answered from his phone.
7. **Edit-capable workers** (only when the owner asks): needs a design — under the cautious policy every Claude edit prompts for approval (trap 5), and the project has **no automated tests** to prove a fix (only `chapter-check` = `tsc --noEmit`). Build the test suite first (`testing-proposal.md`, phases 1–2 are zero prod risk).
   - **CORRECTION 2026-09-20 — "diff the copy against the real folder and carry the changes across" IS UNSAFE, and an earlier session proposed it anyway.** Caught by the owner's GPT session; it is right. `chapter-refresh-copy` is one-way, so after the refresh at T0 the copy drifts by OpenClaw's edits **and the real folder drifts by the owner's own edits**. A two-way `copy ↔ real` diff shows both, mixed together, with no way to tell whose change is whose — and applying it could silently revert the owner's newer work. Timestamps do not fix this, and neither does "only apply files OpenClaw touched" (it may touch a file the owner also edited).
   - **The safe design is a three-way merge against a saved base.** Before a job, keep a pristine snapshot of the state the copy was made from (`chapter-base`, or a git commit/tag in the real repo recording T0). Then: *OpenClaw's changes* = `base → copy`; *the owner's changes* = `base → real`; apply only the first onto the real folder and **surface every overlap as a conflict** instead of picking a winner. `git apply --3way` (or a branch from the T0 commit plus a normal merge) does this properly, which also gives review and undo for free. **The copy has no `.git` on purpose, so the base snapshot has to be kept deliberately — it does not exist today.**
   - **Until that is built there is nothing to carry back**, because workers are read-only. Do not build the transfer before the tests: the carry-back is plumbing, the tests are what make an AI-written fix trustworthy in a live-payments codebase.
   - **Report intake, which is the owner's actual friction, is solved without any of this**: the `openclaw-fixes` skill (`.claude/skills/openclaw-fixes/`) reads the newest report straight from `~/.openclaw/workspace/reports/`, re-checks each finding against current code, settles UNCERTAIN ones by running the test OpenClaw cannot, and hands to `/ship`.

8. **⚠ VERIFY THE SAFETY CHECKS — NOT RUN AFTER 2026-09-20's CONFIG CHANGES. Do this before the next real job.** §6 says to re-run the fence tests after **any** change to models, agents or policy, and that session changed a great deal: `sort`/`uniq` removed from the allowlist, researcher moved to Luna, `agents.defaults.subagents.model` changed, four `thinkingDefault` values set, the whole heartbeat block rewritten, `agents.entries.main.name` set, and `AGENTS.md` edited. **None of the §6 tests were re-run afterwards.** Do:
   - `openclaw security audit` — expect **0 critical, 3 warn** (the three known ones in §6). Anything else is new and caused by those changes.
   - `openclaw doctor` — never run in that session; it may also migrate the obsolete `HEARTBEAT.md` convention. Read its output before using `--fix`.
   - Re-run the §6 worker fence spot-check (a `touch` or `curl` from a worker should be BLOCKED).
   - Confirm from a log line, not from config, that the researcher really runs on Luna: `openclaw logs … | grep "cli turn\|codex"` after one spawn (trap 4).

9. **VERIFY THE HEARTBEAT FIX ACTUALLY WORKED — needs one day of data.** The owner measured **4–6% of the 5-hour ChatGPT window per heartbeat run** *before* the fix. After `isolatedSession: true` (+ Luna, 2h, lightContext, fast-path prompt) it should be a small fraction of that. **Method: note `openclaw models status` at a known time, leave it idle, and compare a few hours later, dividing by the number of 2h ticks in between.** If it is still expensive, the next step is the dedicated cheap heartbeat agent described in §7 — and **test that it can actually wake `main`** before trusting it, because the wake mechanism is undocumented.

10. **Compact the bloated main session — NOT DONE.** On 2026-09-20 `agent:main:main` held **40k/258k tokens** with a **~3.2 MB sqlite + ~4 MB WAL** store, largely from months of non-isolated heartbeat turns (GitHub issue #20011). `isolatedSession: true` stops it growing further, but does not shrink what is there. Use `openclaw sessions compact` (check `--help` first) or `/compact`; community reports ~47% cost reduction from clearing accumulated history. Harmless to defer, but do it before a long job.

11b. **HOW TO STOP RE-FLAGGING SETTLED FINDINGS — `META-AUDIT-CONTEXT.md`, added 2026-09-21.** The pilot produced three accurate findings that were all fine in practice, because **a read-only auditor cannot see the database** and the code alone never says whether a conditional branch is actually taken. The durable fix is not "tell the job to ignore X" — that suppresses the next genuine finding in the same code and quietly defeats the independent check.
   Instead `META-AUDIT-CONTEXT.md` lives in the **real project root**, so `chapter-refresh-copy` carries it into the copy automatically (verified: 649 files, readable by workers). It holds three things the code cannot state: **§1 settled findings with the measurement behind each**, **§2 production facts** (India-only, ~6–12 purchases/month, which of the three Purchase paths has a live browser), and **§3 deliberate choices that look like bugs** (full value at advance, raw `fbc`/`fbp`, `country` always `in`, no blank `ln`, PayU `addedon` as `event_time`, balance/test-phone skips, no commission on open events).
   **The rule it carries is the important part:** a finding matching a settled entry is reported as one line `CONFIRMS-KNOWN: <id>` and never costs a verifier turn; a finding that **contradicts** one is a real finding and must be reported in full. So the check stays independent while the noise stops. **Whoever settles a finding adds it there in the same session, with its evidence** — an entry with no measurement behind it silences a check without earning it.

11c. **THE FULL AUDIT PROMPT, 2026-09-21 — supersedes the prompt inside to-do 11 below. PHASES A AND B ONLY; owner cut Phase C.**
   - **Why Phase C was dropped (owner's call, 2026-09-21):** the proposals are the expensive half — 5 full page reads plus the writing — and Phase B's ranked list already tells him which capabilities are worth a closer look. Proposals become a separate, cheap follow-up on the two or three he actually picks. Nothing is lost permanently.
   - **Why the DOC SET was NOT cut, though the owner suggested it.** Measured 2026-09-21: 193 pages / 1,017 KB, of which **65 are one-line "content has moved" notices** (64 in `13 Guides`, leaving it 15 real of 79); `17 Gateway` is 74 pages / 380 KB and all real; README is 55 KB. **But the job never reads the corpus:** Phase B reads the index plus first headings (~55 KB) and Phase A is capped at 20 pages — about 200 KB of the 1 MB. So pre-deleting sections would save almost nothing, while **Phase B's entire question is "what are we not using?"** — pre-judging applicability makes the pruner's blind spot the audit's blind spot, permanently and invisibly. The asymmetry settles it: if a section really is irrelevant, Phase B says `NOT APPLICABLE` in one line; if the pruner was wrong, the finding is gone for good. **What WAS done instead: the stub counts above were put into the prompt as a fact**, so Phase B does not waste turns on redirects or count one as a capability. Same saving, no blind spot.
   - Phase B is explicitly forbidden from opening a capability's pages, and told that **a wrong `NOT APPLICABLE` is this phase's most expensive mistake** — when unsure, mark `NOT USED`.

   ```
   JOB "capi-full" — read-only. Follow AGENTS.md exactly. TWO phases only: A then B. There is no Phase C in this job — do not write proposals, and do not read a capability's pages in depth to evaluate it. Work so that stopping is safe at any moment.

   STEP 0 — RESUME STATE FIRST, BEFORE ANY OTHER WORK.
   Follow /Users/krutesh/.openclaw/workspace/resume/resume-state-contract.md and write /Users/krutesh/.openclaw/workspace/resume/resume-state.json with: schemaVersion 1, jobId "capi-full", authorized true, status "running", checkpointVersion 1, checkpointPath "/Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-full-progress.md", attemptStartedAt = now. Say in ONE line that you wrote it. If you cannot write it, stop and say so.

   STEP 1 — Run chapter-refresh-copy. Then read IN FULL:
     /Users/krutesh/OpenClaw-Workspace/chapter-copy/META-AUDIT-CONTEXT.md
   It lists what an auditor cannot learn from code: findings already settled, production facts, and deliberate choices that look like bugs. Paste its §1 and §3 tables into EVERY worker task.
     - A finding that MATCHES a settled entry → report it as one line `CONFIRMS-KNOWN: <id>` and do NOT send it to the verifier.
     - A finding that CONTRADICTS a settled entry → that is a real finding. Report it in full and say which entry it contradicts.
     - Anything not covered there → normal finding, normal verification.

   WHERE WORK ACCUMULATES. After each phase, append that phase's finished section to
     /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-full-working.md
   and update the progress file. Write the FINAL report only when BOTH phases are done — a final report appearing early tells Krutesh the job has finished when it has not.

   PHASE A — IS WHAT WE BUILT CORRECT? (start from the CODE, never the docs)
   A1. A researcher derives the real surface: run `grep -rl "_shared/metaCapi" /Users/krutesh/OpenClaw-Workspace/chapter-copy/supabase/functions/`, OPEN EVERY MATCH and keep only genuine importers (a comment naming the file is not an importer), then read `_shared/metaCapi.ts` and each importer. Report every endpoint, event name, parameter, field and API version we actually send, each with a `file:line`.
   A2. ONLY THEN pick the documentation pages that cover exactly those things — **at most 20 pages, never whole folders** — from `/Users/krutesh/Downloads/Conversions API Docs` (index in its README.md). Paths contain spaces, so quote them: cat "/Users/krutesh/Downloads/Conversions API Docs/03 Using the API.md"
   A3. Split those pages between Researcher A (openai/gpt-5.6-luna) and Researcher B (anthropic/claude-sonnet-5). Question: where does what we send disagree with what the page requires? Required fields omitted, values formatted against the page's stated rule, anything the page calls deprecated.
   A4. Verify every candidate with the verifier before believing it.

   PHASE B — WHAT ARE WE NOT USING? (start from the DOCS index — code never mentions what it does not do)
   Read README.md's index and the first heading of each top-level section ONLY. **Do not open a capability's pages to evaluate it** — the index line and its first heading are all you get, and that is deliberate.
   **Known shape of this doc set, measured 2026-09-21 — use it, do not re-derive it.** 193 pages, but **65 are one-line "This content has moved" notices**, 64 of them inside `13 Guides`: that folder has 79 pages and only **15 real** ones. Meta moved the Gateway material into `17 Conversions API Gateway and Signals Gateway` (74 pages, all real). A few more pages are blank on Meta's own site (the AWS EKS onboarding set) and `14 Payload Helper.md` is an interactive form with only intro text. **Do not count a moved-notice or a blank page as a capability**, and do not spend turns reading them. The README says all of this in its "Notes on this copy" section.

   Produce ONE table covering every Conversions API capability, with these columns:
     capability | USED / NOT USED / NOT APPLICABLE | what it does, one line | where it lives (file or folder)
   Judge applicability against META-AUDIT-CONTEXT.md §2: chapter அ is a small India-only social-experiences booking business that sells event tickets, takes payment through PayU, messages customers on WhatsApp, has no mobile app, and runs roughly 6–12 ticket purchases a month. **Say WHY when you mark something NOT APPLICABLE** — one clause is enough, and a wrong "not applicable" is the most expensive mistake in this phase, so when you are unsure mark it NOT USED rather than ruling it out.

   Then, below the table, list the **NOT USED rows ranked by how promising they look for this business**, one line each saying what makes it promising or not. Ranking only — **no proposals, no build plans, no page reads.** That list is what Krutesh will pick from for a separate follow-up job.

   REPLY CAPS. Every worker task: at most 8 items, at most 3 lines each. Phase A findings in exactly this shape:
     claim | doc file + quoted line | code file:line | one-line reason
   A Phase A claim without BOTH a doc quote and a code file:line is not a finding — drop it.

   BUDGET AND STOPPING.
   Check `openclaw models status` before each phase and between researcher batches. As the ChatGPT 5-hour allowance falls below about a third, send new research to Researcher B and keep the rest for your own orchestration. If a limit stops you: append finished work to the -working file, update progress, set the resume state to waiting_for_limit with blockedProvider and the EXACT reset time, and stop cleanly. Never switch models to get around a limit.

   FINAL REPORT — only when both phases are complete.
   /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-full.md, in this order:
     1. What we send today — the Phase A map, including how many real importers there are.
     2. "What is wrong" — Phase A findings, each labelled (COMPLIANT / PARTIALLY COMPLIANT / NON-COMPLIANT / OBSOLETE / IRRELEVANT / UNCERTAIN) with evidence and the verifier's verdict. List CONFIRMS-KNOWN ids separately, as one line, not as findings.
     3. "What we are not using" — the Phase B table, then the ranked NOT USED list.
     4. Whether the co-manager AGREED or DISAGREED with your plan.
     5. What is uncertain, and what needs Krutesh's decision.
     6. "How this run went" — what worked first time, what was blocked, what you worked around, and whether a limit stopped you at any point.
   Then set the resume state to completed with authorized false, give a short plain-language summary in chat, and STOP.
   ```

11. **RUN THE CONVERSIONS API AUDIT — the next real work. ⚠ START SMALL (owner's plan, 2026-09-21): run a narrow audit of 5–10 documents FIRST**, not the full three-phase job. Rationale: nearly everything in this system is unproven (see the START HERE list), and a small job exercises the same machinery — plan, delegate, verify, report, and possibly the limit/resume path — at a fraction of the budget. **If it works end to end, run the full prompt below. If it stalls, you have lost almost nothing.** Full three-phase prompt: It is the second half of to-do 3 and the job OpenClaw exists for. **No internet needed:** all 193 doc pages are on disk at `/Users/krutesh/Downloads/Conversions API Docs` with a full index in its `README.md`. The owner wants it to be **two jobs in one — compliance AND opportunity** ("I don't want them to just correct what is already built"), which matters because they need *opposite* reading strategies: an audit starts from the code and looks up matching docs, while finding unused features must start from the docs index, since the code never mentions what it does not do. Hence the phases. **Paste this into the Manager chat in the dashboard** (not through a Claude session — see §3 on budget):

   ```
   JOB: "capi-audit" (read-only). Follow AGENTS.md exactly, including the two-manager rule at all three decision points, and paste the read-only boilerplate into EVERY worker task.

   AUTO-RESUME — DO THIS FIRST, BEFORE ANY RESEARCH. This job must be resumable without me. Write /Users/krutesh/.openclaw/workspace/resume/resume-state.json with: schemaVersion 1, jobId "capi-audit", authorized true, status "running", checkpointVersion 1, checkpointPath set to this job's -progress.md path under /Users/krutesh/.openclaw/workspace/reports/, attemptStartedAt now. Follow the contract in ~/.openclaw/workspace/resume/resume-state-contract.md exactly. If a usage limit stops you: save progress, then set status "waiting_for_limit", blockedProvider to the provider that actually blocked you (openai or anthropic — they are separate allowances), and notBefore to the EXACT reset time from the error message. If the error gives no exact reset time, set "waiting_for_user" and stop — never guess a time. On completion set "completed" and authorized false. No internet is needed — all docs are on disk at /Users/krutesh/Downloads/Conversions API Docs (start from its README.md, which is a full index of all 193 pages).

   This job has TWO goals. Keep them separate and do them in this order.

   PHASE A — IS WHAT WE BUILT CORRECT?
   Start from the code, not the docs. Have a researcher derive the real list of what we send: run `grep -rl "_shared/metaCapi" supabase/functions/` to find candidate files, OPEN EACH MATCH and keep only real imports (a comment that merely names the file is not an importer), then read supabase/functions/_shared/metaCapi.ts and each importer, and list every endpoint, event name, parameter, field and API version we actually send. Only then pick the doc pages covering those exact things (mainly "03 Using the API", "05 Parameters", "12 Handling Duplicate Events", "15 Best Practices", "04 Verifying Setup"). Never read a whole folder. Label each finding COMPLIANT / PARTIALLY COMPLIANT / NON-COMPLIANT / OBSOLETE / IRRELEVANT / UNCERTAIN. Rank by risk to money, customers and ad measurement. Save progress, then continue.

   PHASE B — WHAT ARE WE NOT USING?
   Read README.md's index and the first heading of each top-level section ONLY. Produce a table of every Conversions API capability, marked USED / NOT USED / NOT APPLICABLE, with one line each on what it does. Do not read the pages in depth yet. Note explicitly that chapter அ is a small Indian social-experiences booking business: it sells event tickets, takes payment via PayU, messages customers on WhatsApp, has no mobile app, and collects some balances in person at the venue. Mark NOT APPLICABLE aggressively — most pages will be. Save progress, then continue.

   PHASE C — WHAT IS WORTH BUILDING?
   For at most the 5 most promising NOT-USED capabilities from Phase B, read those specific pages properly and write a short proposal each: what it is in plain language, what it would do for this business, what we would have to build, what it depends on, and how confident you are. Flag anything that needs the owner's judgement rather than deciding it.

   RULES:
   - Do NOT use `pwd`, `sort`, `uniq`, `sed`, `awk` or `find` — none are allowlisted. One command per call: no pipes, no `&&` chaining. A compound command is denied and costs ~2 minutes.
   - Every worker must reply with AT MOST 8 items, each AT MOST 3 lines: claim | doc file + quoted line | code file:line | one-line reasoning. Nothing else — long prose is truncated before the manager sees it.
   - Every Phase A finding needs BOTH a doc quote AND a code file:line.
   - Verify every Phase A finding with the verifier before believing it.
   - Do not propose or make any code change. Read-only; the owner's own session implements fixes.
   - Check usage before each phase. If a limit stops you, save progress and stop cleanly.

   OUTPUT: progress file ~/.openclaw/workspace/reports/<date>-capi-audit-progress.md updated after EACH phase. Final report ~/.openclaw/workspace/reports/<date>-capi-audit.md with two clearly separated sections: "What is wrong" (Phase A) and "What we could build" (Phases B and C).
   ```

   **Known good candidates for Phase B**, spotted while indexing the docs (do not feed these to the job — let it find them, then compare): `09 Conversions API for Business Messaging` (he runs WhatsApp via AiSensy), `08 Offline Events` (Pay-at-Venue balances), `11 Dataset Quality API` (EMQ is tracked by hand today), `10 Conversion Leads Integration`.

11a. **THE PILOT — RUN 2026-09-21 05:55 IST. PASSED.** This is to-do 11's "start small": six documents, one question — *does what we send match the spec?* Phase A only; Phases B and C stay in to-do 11 for the full job.

   **RESULT — the machinery works. Report: `~/.openclaw/workspace/reports/2026-09-21-capi-small.md` (10 KB), findings re-verified against the live repo and recorded in `META-ADS-HANDOFF.md` §23.**
   - **The planted decoy was PASSED**: it reported **five** real importers, named them, and stated why the other two grep hits were excluded (self-match, and `meta-signal-watchdog`'s comment-only mention). The researcher genuinely opened the files.
   - **Four things ran for the first time ever:** the manager wrote `resume-state.json` and closed it `completed`; both researchers ran in parallel on different model families; the trimmed plan-only co-manager rule executed; and `report-ready-notify` delivered to Telegram off a real job.
   - **The co-manager DISAGREED** — it wanted `05 Parameters/02 Main Body Parameters.md` as a seventh page. The manager overruled it and **was right**: that page is two rows, both already sent correctly. Real signal, not rubber-stamping — one data point for the ratio the owner is tracking.
   - **Cost ~9% of a 5-hour ChatGPT window** (36% → 27%), for two verified real defects. That is the number that makes the full audit affordable.
   - **Quality caveats, recorded honestly:** Researcher A (Luna) returned **zero** candidates from its three pages; all three findings came from Researcher B (Sonnet) — one data point, worth watching. One citation slip (cited line 42 for an example that is on line 41). **Trap 18 bit again even with reply caps** — the co-manager's full response could not be recovered from truncation. Researcher B attempted a forbidden shell pipeline; the runtime denied it and the report disclosed it rather than hiding it.
   - **What the report could NOT do, by design:** measure how often the missing user-agent actually occurs, or check the browser side. The second mattered — `src/metaPixel.ts` normalises names identically, so the accent fix is two-sided. **This is the structural lesson again: a read-only auditor hands back findings, not conclusions.** Closing them is `/openclaw-fixes` in an interactive session.

   **Design notes, so nobody "improves" it back into the thing it avoids:**
   - **It names the six doc pages instead of letting the manager map its own.** AGENTS.md step 1 normally has the manager pick the pages, and for the full audit it should. Here the point is to prove the *machinery*, not to discover the best doc set: a fixed scope makes the budget predictable and the output gradeable.
   - **The six were chosen against the code's actual surface** (`sha256Hex`, `normaliseEmail`, `normalisePhone`, `normaliseNamePart`, `sendPurchaseToMeta`, `sendLeadToMeta`, fbp/fbc, value/currency), and split ~41 KB / ~33 KB between the two researchers. `05 Parameters/02 Main Body Parameters.md` was deliberately excluded — it is an 806-byte stub.
   - **Step 2(b) is a planted decoy, like the verifier bake-off.** `grep -rl` returns **7** files; one is the module itself and `meta-signal-watchdog/index.ts` only names it in comments (trap 20). **The right answer is 5 real importers** — `payu-callback`, `payu-webhook`, `verify-pending-payments`, `capi-lead`, `meta-audience-sync` — and `v26.0` at `metaCapi.ts:60`. **A report saying 6 or 7 means the researcher skimmed, and every doc finding in it deserves less weight.** Do not put the expected answer in the prompt.
   - **The boilerplate-paste line was ALSO removed once the workers got their own `AGENTS.md` (2026-09-21).** It existed only because workers had no standing rules; the fix made it redundant. Fixing the system shortened the prompt — that is the right direction, not the reverse.
   - **It carries no RULES block.** An earlier draft repeated the budget check, the usage-limit procedure and the read-only shell rules — all of which the manager already reads in AGENTS.md — and one line ("do not propose any code change") **contradicted** AGENTS.md's own report format, which asks for recommended fixes. Removed. The one instruction kept is the boilerplate paste, because of the 2026-09-21 finding that workers load only their own workspace's AGENTS.md.
   - **Do not attach the Meta documents.** The agents open them with `cat`; attaching ~74 KB into the *manager's* context is the opposite of what is wanted, since the manager never needs to read them — the researchers do, in their own isolated contexts.

   **Watch for, when it finishes:** the importer count (above); whether `resume-state.json` was actually written (never done before); whether the co-manager is recorded as AGREED or DISAGREED; and **whether a Telegram message arrives** — `report-ready-notify` is armed and baselined on `2026-09-20-heartbeat-research-chatgpt.md`, so a new final report should trigger the first delivery this bot has ever made.

   ```
   JOB "capi-small" — read-only. Follow AGENTS.md exactly. This is a deliberately SMALL first end-to-end test of the team, not the full Conversions API audit: six documents, one question. Do not widen the scope, do not read other doc pages.

   STEP 0 — RESUME STATE FIRST, BEFORE ANY OTHER WORK.
   Follow /Users/krutesh/.openclaw/workspace/resume/resume-state-contract.md and write /Users/krutesh/.openclaw/workspace/resume/resume-state.json with: schemaVersion 1, jobId "capi-small", authorized true, status "running", checkpointVersion 1, checkpointPath "/Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-small-progress.md", attemptStartedAt = now. Then say in ONE line that you wrote it. If you cannot write it, stop and say so.

   STEP 1 — Run chapter-refresh-copy.

   STEP 2 — MAP WHAT WE ACTUALLY SEND. Spawn Researcher A (agentId "researcher", model "openai/gpt-5.6-luna"). Its task:
     a) Run: grep -rl "_shared/metaCapi" /Users/krutesh/OpenClaw-Workspace/chapter-copy/supabase/functions/
     b) OPEN EVERY MATCH and keep only the files that genuinely import that module. A file that merely names it in a comment is NOT an importer. Report how many real importers there are and name them.
     c) Read /Users/krutesh/OpenClaw-Workspace/chapter-copy/supabase/functions/_shared/metaCapi.ts and report the Graph API version with its file:line.
     d) List every field we send for the Purchase event and for the Lead event, each with a file:line.
     Reply cap: at most 8 items, at most 3 lines each. No prose.

   STEP 3 — PLAN, then the ONE co-manager consultation. Write your own plan for Step 4 first. Then send the co-manager (agentId "comanager", model "anthropic/claude-opus-5") the Step 2 facts and this job's scope, WITHOUT showing your plan, and ask for its independent position on whether these six pages are the right six to answer "does what we send match the spec?". Record AGREED or DISAGREED in those words. One round of rebuttal at most; if you still disagree, put both positions in the report and carry on with the six pages.

   STEP 4 — READ THE DOCS. Both researchers, in parallel. Every path below contains spaces, so quote it, e.g.
     cat "/Users/krutesh/Downloads/Conversions API Docs/03 Using the API.md"
   Researcher A (agentId "researcher", model "openai/gpt-5.6-luna"):
     /Users/krutesh/Downloads/Conversions API Docs/03 Using the API.md
     /Users/krutesh/Downloads/Conversions API Docs/05 Parameters/03 Server Event Parameters.md
     /Users/krutesh/Downloads/Conversions API Docs/12 Handling Duplicate Events.md
   Researcher B (agentId "researcher2", model "anthropic/claude-sonnet-5"):
     /Users/krutesh/Downloads/Conversions API Docs/05 Parameters/04 Customer Information Parameters/01 Customer Information Parameters.md
     /Users/krutesh/Downloads/Conversions API Docs/05 Parameters/04 Customer Information Parameters/03 fbp and fbc Parameters.md
     /Users/krutesh/Downloads/Conversions API Docs/05 Parameters/05 Standard Parameters.md
   Give each researcher the Step 2 field list as established fact, so neither re-reads the code. The single question for both: WHERE DOES WHAT WE SEND DISAGREE WITH WHAT THESE PAGES REQUIRE? Include required fields we omit, values we format differently from the page's stated rule, and anything the page calls deprecated.
   Reply cap for each: at most 8 findings, at most 3 lines each, in exactly this shape:
     claim | doc file + quoted line | code file:line | one-line reason
   Nothing else. A claim without BOTH a doc quote and a code file:line is not a finding — drop it.

   STEP 5 — VERIFY. Send every candidate finding to the verifier (agentId "verifier", model "claude-cli/claude-opus-4-7"): the claim, the doc quote and the code file:line ONLY — never the researcher's reasoning. It answers CONFIRMED, REJECTED or UNCERTAIN with its own evidence. Do not report any finding the verifier has not seen.

   STEP 6 — REPORT AND STOP.
   Progress file, updated after EACH step: /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-small-progress.md
   Final report: /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-small.md, in this order:
     1. What we send today — the Step 2 map, including how many real importers there are.
     2. Findings, each with a label (COMPLIANT / PARTIALLY COMPLIANT / NON-COMPLIANT / OBSOLETE / IRRELEVANT / UNCERTAIN), its evidence and the verifier's verdict.
     3. Whether the co-manager AGREED or DISAGREED with your plan.
     4. What is uncertain, and what needs Krutesh's decision.
     5. "How this run went" — which steps worked first time, what was blocked, anything you had to work around.
   Then set the resume state to status "completed" with authorized false. Then give a short plain-language summary in chat and STOP. Do not start anything else.
   ```

11d. **THE PROPOSALS JOB — the Phase C that was cut out of 11c, as its own run. Drafted 2026-09-21, NOT YET RUN.** Owner's call: `capi-full` ran A+B only, and its ranked NOT USED list is the input here.
   - **Why it is a separate job at all:** Phase B ranked capabilities from titles and headings without opening a single page, which is cheap but thin. This job's whole purpose is to actually read those pages — so its ranking is allowed to **overrule** Phase B's. It may demote one capability and take the next down the list, provided it records the page evidence that demoted it. A demotion is a result, not a failure.
   - **The failure mode it is built against:** proposing to build something that already exists. This project carries far more Meta and messaging plumbing than a first read suggests, so the prompt makes checking the existing code a numbered step, with the exact greps. ⚠ It also warns that **the migration FILES are an incomplete record of the live database** (`META-ADS-HANDOFF.md` §24 item 8: 273 applied vs 190 files), so a missing file is never evidence a table does not exist — the job must say "not found in the repo" and leave it to the owner.
   - **Two owner rules written into it explicitly**, because a generic model breaks both: **low volume is not an argument against laying plumbing** (data not captured on day 1 cannot be backfilled — say the caveat once and still propose it), and **never tell him to launch, spend or move faster.** He sets the pace.
   - **The verifier gets facts only, never the recommendation.** "Meta requires X" and "we already have Z at `file:line`" are checkable; whether something is worth building is the owner's judgement, and sending it to a verifier would dress an opinion up as a verdict.

   ```
   JOB "capi-proposals" — read-only. Follow AGENTS.md exactly. This is the follow-up to "capi-full": turn its ranked list of unused capabilities into real proposals. Read-only; propose, never build.

   STEP 0 — RESUME STATE FIRST, BEFORE ANY OTHER WORK.
   Follow /Users/krutesh/.openclaw/workspace/resume/resume-state-contract.md and write /Users/krutesh/.openclaw/workspace/resume/resume-state.json with: schemaVersion 1, jobId "capi-proposals", authorized true, status "running", checkpointVersion 1, checkpointPath "/Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-proposals-progress.md", attemptStartedAt = now. Say in ONE line that you wrote it.

   STEP 1 — Run chapter-refresh-copy. Then read:
     /Users/krutesh/OpenClaw-Workspace/chapter-copy/META-AUDIT-CONTEXT.md   (in full — §2 is the business, §3 is what not to re-litigate)
     /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-full.md     (section 3 only — the capability table and the ranked NOT USED list)
   Paste META-AUDIT-CONTEXT §1 and §3 into EVERY worker task.
   **Intermediate files go in ~/.openclaw/workspace/reports/working/**, never beside the final report.

   WHAT TO PROPOSE
   Take the **top 5** from that ranked NOT USED list. The ranking was made from titles and headings only, so it is a starting point, not a verdict.
   **You may demote one.** If reading a capability's actual pages shows it does not deserve a proposal for this business, say so in two lines, drop it, and take the next one down the list. Record every demotion and why — a demotion backed by the real pages is a useful result, not a failure.

   FOR EACH capability, in this order:
   1. **Read its actual pages properly.** This is the new information this job exists to gather — capi-full deliberately read none of them.
   2. **Check what already exists here before claiming anything must be built.** This project has more Meta and messaging plumbing than a first read suggests. Cheap ways to find out, from the project copy:
      `grep -rl "<keyword>" /Users/krutesh/OpenClaw-Workspace/chapter-copy/supabase/functions/`
      `ls /Users/krutesh/OpenClaw-Workspace/chapter-copy/supabase/migrations/`
      `grep -rl "<keyword>" /Users/krutesh/OpenClaw-Workspace/chapter-copy/src/`
      **A proposal that says "we would need to build X" when X already exists is the worst failure mode of this job.** ⚠ The migration FILES are an incomplete record of the live database (see below), so absence of a file is NOT evidence a table does not exist — say "not found in the repo" and leave it for Krutesh, never "does not exist".
   3. Write the proposal, at most **12 lines**, in this shape:
      - **What it is** — plain language, no Meta jargon. Krutesh is a no-code founder; define every term on first use.
      - **What it would do for chapter அ specifically** — tied to this business, not to businesses in general.
      - **What we would have to build**, and what already exists (with `file:line` or a table name where you found it).
      - **What it depends on** — Meta-side setup, eligibility, an account or permission, another capability first.
      - **Confidence, and what would prove you wrong.** Say plainly when the pages do not settle something.

   RULES OF JUDGEMENT
   - **Volume is not an argument against laying plumbing.** The business runs ~6–12 ticket purchases a month. If a capability only pays off at higher volume but the data has to be captured from day one to be usable later, **say that plainly and still propose it** — data not captured is gone and cannot be backfilled. Equally, do not dress a low-volume capability up as urgent.
   - **Do not tell Krutesh to launch, spend, or move faster.** He sets the pace. Propose what could be built; the timing is his.
   - **Flag anything needing his judgement rather than deciding it** — cost, brand, whether a channel is wanted at all.
   - **No build plans, no code, no schemas.** A proposal says what and why, not how.

   REPLY CAPS. Every worker task: at most 8 items, at most 3 lines each. Split the five capabilities between Researcher A (openai/gpt-5.6-luna) and Researcher B (anthropic/claude-sonnet-5); never give both the same one by accident.

   VERIFICATION. Send the verifier only the factual claims — "Meta requires X", "this capability does Y", "we already have Z at file:line" — never the recommendation itself. A proposal's *judgement* is yours and Krutesh's, not the verifier's. It answers CONFIRMED / REJECTED / UNCERTAIN.

   BUDGET. Check with `chapter-budget` before each capability. Do NOT run `openclaw models status` — it is not allowlisted and stalls ~90s. If a limit stops you: save finished proposals to the -working file, update progress, set waiting_for_limit with blockedProvider and the exact reset time, stop cleanly.

   FINAL REPORT — /Users/krutesh/.openclaw/workspace/reports/2026-09-21-capi-proposals.md
     1. One-paragraph summary a busy person can read in 20 seconds.
     2. The proposals, ranked, best first — and say what the ranking is by (effort, likely gain, or dependency order — name which).
     3. Demoted capabilities and why, with the page evidence that demoted them.
     4. What needs Krutesh's decision, as a short list of actual questions.
     5. What is uncertain, including anything the pages did not settle.
     6. "How this run went" — what worked, what was blocked, whether a limit stopped you.
   Then set the resume state to completed with authorized false, give a short plain-language summary in chat, and STOP.
   ```

12. **When the report lands, run `/openclaw-fixes` in an interactive Claude session.** That skill (`.claude/skills/openclaw-fixes/SKILL.md`, created 2026-09-20, **uncommitted**) reads the newest report straight off disk — the owner never copies anything — re-checks each finding against the *current* code (reports go stale and researchers make mistakes: three in the safety review), settles UNCERTAIN findings by running the test OpenClaw is forbidden to run, then fixes with his approval and hands to `/ship`.

13. **PARTLY DONE 2026-09-20 — the gate's DECISION LOGIC is now proven, 16/16, at zero model cost.** A standalone harness runs `resume-gate.js` with shimmed OpenClaw globals (`trigger`, `exec`, `json`) so it never touches the real state file and never fires the automation. **Preserved at `~/.openclaw/workspace/resume/gate-harness.mjs` — re-run after ANY change to the gate with `node ~/.openclaw/workspace/resume/gate-harness.mjs`.** All 16 pass: idle · empty file · malformed JSON (`gate-error`) · `authorized:false` · `ready` FIRES · `running` blocked · `waiting_for_limit` with a future reset blocked (`limit-window-pending`) · with a past reset FIRES (`limit-window-reached`) · with no `notBefore` blocked (`limit-window-unknown`) · completed/paused/waiting_for_user blocked · **`checkpointPath` outside `reports/` rejected (path-traversal guard works — `/etc/passwd` refused)** · `checkpointVersion 0` rejected · `schemaVersion 2` rejected · **duplicate suppression confirmed (fires once, second tick returns `already-fired`)**. It fails closed on every malformed input.
    **Restart safety: strong evidence, not proven.** `triggerState` (including `lastFiredFingerprint`) is stored in the scheduler's job record in SQLite, not in memory, so it should survive a gateway restart — but an actual restart test was not run.
    **STILL UNTESTED, and these are the ones that cost money or matter most:** (a) **a real Astra handoff** — the fire path has never launched an actual manager run, only a dummy payload; (b) **the manager actually writing `resume-state.json` correctly when a limit hits** — AGENTS.md lines 176–180 now instruct it and the audit prompt in to-do 11 sets the state explicitly, but that path has never once executed; (c) **Mac sleep/wake recovery**; (d) **checkpoint-vs-source consistency** (the real project may change while a job is paused — do not silently mix old evidence with refreshed code); (e) the **`no-route`** delivery failure found in the old heartbeat receipts. **The first real audit is therefore also the first real test of resume — expect to check on it rather than assume it worked. Nothing is lost if it stalls: the progress file is on disk and the job can be restarted by hand.**
   ⚠ ORIGINAL NOTE: **VALIDATE THE NEW RESUME GATE BEFORE TRUSTING IT UNATTENDED (§7a).** It replaced the heartbeat but its validation stopped early. Cover at minimum: **one real Astra handoff** end to end; **no duplicate attempt** when the gate ticks while a manager or its workers are still running; **recovery after a gateway restart and after Mac sleep/wake** without launching a backlog; and **checkpoint-vs-source consistency** (the real project may have changed while a job was paused — do not silently mix old evidence with refreshed code). Also resolve the **`no-route`** receipt the research found, since a delivery failure looks exactly like a cheap quiet check. **Most of this can be tested with fabricated local state and a fake payload, costing no model credits** — do that before any real run.
14. **DONE 2026-09-21 — all autonomous Skill Workshop learning and Memory Dreaming are OFF by owner decision.** The original inventory found **four weekly `skill-collection-review` jobs, one per agent** (main, researcher, verifier, comanager), each an `agentTurn` with a ~3,200-char prompt **on that agent's own model — so two of them were weekly Opus 4.7 and Opus 5 runs against the scarce Claude pool**, for agents that only read and judge and never author skills. Adding `researcher2` later created a fifth system-owned definition. Worse, each payload carried `toolsAllow: ['ls','read','write','edit','apply_patch','exec','process']` — **`write`/`edit`/`apply_patch` on agents whose config denies exactly those**. (OpenClaw's stated rule is that `deny` "blocks listed tools even when profile or provider rules would allow them", and the exec-policy precedent in trap 22 shows the stricter rule winning, so the deny very likely held — but it was never proven, and leaving the jobs disabled removes the question.)
    **Final decision:** set **`skills.workshop.autonomous.mode: "off"`**. `"propose"` was enabled briefly, then disabled after confirming that a qualifying substantial foreground conversation can trigger an additional detached review on that agent's configured model. There is no weekly proposal schedule, but the extra event-driven review can still spend ChatGPT or Claude tokens. `"off"` disables autonomous capture and immediate autonomous repair; explicit `/learn` and manual Workshop requests remain available. Live verification found all five `skill-collection-review:*` definitions at `enabled: false`, `status: disabled`. No learned skills, proposals, experience reviews, or collection-review history existed when it was turned off.
    **These are system-owned monitor jobs: `openclaw automations disable <id>` fails with "system-owned monitor jobs cannot be edited by cron clients".** Control them through `skills.workshop.autonomous.mode`, not individual automation edits. The original unset value meant default `"auto"`, which applied captured proposals and ran weekly reviews by itself. `chapter-auto-resume` remains separate and was unaffected.
    **Memory Dreaming was also disabled:** `plugins.entries.memory-core.config.dreaming.enabled: false`. It previously ran a unified daily 03:00 sweep that could call models for Light/REM/Deep memory consolidation and promote repeated material into `MEMORY.md`. At shutdown, the main workspace had eight short-term entries, zero promoted entries, the other workspace had no short-term store, and every OpenAI embedding probe failed with `credit_balance_exhausted`. The managed `memory-core:memory-dreaming-promotion` automation disappeared after the setting changed. Re-enable only by an explicit owner decision after embedding/search health and the desired model budget are settled.
   ORIGINAL NOTE: **Inventory the OTHER recurring jobs before calling the idle system cheap.** `openclaw automations list` shows `memory-core:memory-dreaming` (daily 03:00) plus **four** weekly `skill-collection-review` jobs (main, researcher, verifier, comanager). None has been costed. The heartbeat was fixed; these were never measured, and one of them may now be the largest idle consumer.

15. **SYMMETRIC DUAL-ORCHESTRATOR — designed, DEFERRED by the owner 2026-09-21 until the current machinery is proven. Do not build it before to-do 11 has run.** The owner correctly identified a real flaw: **the asymmetry in §7a is an accident, not a principle.** If Claude is empty the job continues; if ChatGPT is empty *everything* stops, purely because the only orchestrator lives there. His proposal — and it is architecturally sound — is that **Opus 5 should be able to drive the job when ChatGPT is out**: claim the job, delegate reading to Researcher B, verify with Opus 4.7, and hand back to Astra for the final report and a two-manager reconciliation when ChatGPT returns.
    **Why it is feasible:** Opus 5 is a legitimate orchestrator (the literature's bar is *"the two strongest models"*), and **the hard part already exists** — `status: running` in the resume contract is a mutual-exclusion claim, so whichever manager claims the job owns it and the other stands down.
    **What it needs:** (a) a **second gate automation** whose payload model is `anthropic/claude-opus-5`, firing only when the state says `blockedProvider: openai` (the existing gate's payload model is hardcoded to Astra and cannot be made dynamic); (b) both gates continuing to respect `status: running`; (c) an AGENTS.md protocol for the standby orchestrator, which must stamp **"PLAN NOT INDEPENDENTLY REVIEWED"** for the same reason the reverse case does — when Opus 5 drives, Astra is not there to review *its* plan.
    **Why it was deferred, and the owner agreed:** today the asymmetry costs **delay, not data** — the gate resumes automatically, worst case after one reset window. Building a second orchestrator now would stack an untested path on top of a resume path that has never once run, making failures nearly impossible to attribute. **Run to-do 11 first.** If overnight autonomy later proves valuable, a lost night to a 5-hour stall is the argument for building this.

---

## 10. Owner's rules that bind every agent and every session

- **Nothing is pushed until he says so, in that turn** — `CLAUDE.md` golden rule 2. Before any push, list `git log origin/main..HEAD` and show him what would go out.
- **Interactive Claude sessions commit only with his OK.** OpenClaw does not commit at all any more (no git in the copy).
- **Deploys and prod DB changes do not need a per-change OK** (he said per-change interruptions defeat hours-long autonomy), but **OpenClaw has no live access**, and when it ever gets some the payment-path functions stay excluded.
- **Do not nudge him toward launching or shipping faster.** He sets the pace; the work is building and perfecting systems.
- **Explain in plain language.** Define every term on first use. He is learning to read code, not write it.
