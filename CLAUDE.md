# chapter அ — Claude Code guide

Mobile-first social-experiences booking webapp (React + Vite + TypeScript, Supabase backend, PayU payments, AiSensy WhatsApp). Deployed from `main`. The owner is a **no-code founder**: explain plans and tradeoffs in plain language; never assume they can edit code or config themselves.

## Golden safety rules
1. **The Supabase DB is PRODUCTION with live customers.** For test rows use phone `90000000xx`, verify writes with `RETURNING`, and delete test rows afterwards. Never mutate rows whose status is `advance_paid`/`fully_paid` without explicit instruction — guard UPDATEs with `status not in ('advance_paid','fully_paid')`.
2. **Pushing to `main` deploys the live site.** Never `git push` without the user's explicit go-ahead in that conversation turn.
3. **Deploying edge functions is allowed** (the deny rules were lifted 2026-08-26) — but two things must never be got wrong.
   - **ALWAYS pass `--no-verify-jwt`** for any function a customer, PayU, or a cron reaches. There is no `supabase/config.toml`, so the CLI defaults `verify_jwt` to **true** when the flag is omitted — deploying `payu-callback` or `payu-webhook` without it makes PayU's unauthenticated POST return 401 and **payments stop**. Currently `verify_jwt: false` (deploy WITH the flag): `create-payu-order`, `payu-callback`, `payu-webhook`, `verify-pending-payments`, `capi-lead`, `cart-abandonment`, `get-user-context`, `open-event-otp`, `retarget-check`, `brevo-webhook`, `send-admin-push`. Currently JWT-verified (deploy WITHOUT it): `send-push-notification`, `send-aisensy-invite`, `send-brevo-invite`, `creator-signup`, `marketer-signup`.
   - **`supabase/functions/_shared/*` is bundled at DEPLOY time**, so editing it changes nothing live until *every* importer is redeployed — and nothing warns you. This silently cost us two Meta reporting gaps (see the `edge-function-shared-module-drift` memory). Importers of `_shared/metaCapi.ts`: `payu-callback`, `payu-webhook`, `verify-pending-payments`, `capi-lead`.
   - After any deploy, verify with the Supabase MCP `list_edge_functions`: `updated_at` must have moved **and** `verify_jwt` must still read what the list above says.
4. **Deploy hold:** none active — the open-event batch (`src/App.tsx`, `src/AppFlow.tsx`, `src/PaymentOverlay.tsx`, `supabase/functions/payu-callback`, `supabase/functions/cart-abandonment`) was shipped on 2026-07-05. Still prefer isolated, one-concern commits and verify with `git status --short` before and after committing. If a new hold is put in place, record its file list and "active" status here.
5. Local `npm run dev` talks to PROD Supabase — UI testing creates real rows and real admin notifications.

## File map (large files — read targeted line ranges, never whole files)
- `src/App.tsx` (~5.4k lines): homepage, INVITE-only flow, `PayUReturnScreen` (payment success/failed/receipt + retry bill), routing shell.
- `src/AppFlow.tsx` (~5.2k lines): `/plans` chat UI, event details overlay + calendar sheet, OPEN-event booking flow.
- `src/AdminPanel.tsx` (~6.6k lines): admin + marketer dashboards — People tab, event/timeline editors, marketer cards. Founder tab row is **Plans · Flow · People · Team · Growth · Build · ⚙** (`ADMIN_TABS`, near the top of the file). Several tabs hold pill sub-views, and the pill is the page's only name — no heading repeats it below:
  - **Flow** (`flowMode`) ▸ Media · Timelines · Doubt answers
  - **People** (`peopleMode`) ▸ Call · Approval · Payments · Doubts
  - **Team** (`teamMode`, tab key is still `'marketers'`) ▸ Performance · Marketers · Managers · Creators
  - **Growth** (`growthMode`, tab key `'analytics'`) ▸ Analytics · Experiments
  Retired tab keys still land somewhere sensible via the `rememberedTab` migration: `affiliates`/`content` → Team ▸ Creators, `manager` → Team ▸ Managers, `experiments` → Growth ▸ Experiments. Sub-view data is fetched from effects keyed on the sub-view, not from the tab button's onClick.
- `src/CreatorOnboardingDemos.tsx` and `src/remotion/`: schematic replicas of the creator dashboard/booking surfaces; refresh both whenever those live surfaces change.
- `src/TeamOnboarding.tsx`, `src/TeamOnboardingLevels.tsx`, and `src/TeamOnboardingMocks.tsx`: core-team onboarding through the customer desk, canonical lesson copy, and practice-only lead simulators; whenever live statuses or marketer flows change, refresh the onboarding mocks and copy too.
- `src/PaymentOverlay.tsx`: shared PayU bill page (`NativePaymentOverlay`) used by both flows.
- `src/ManagerPanel.tsx`: the 6pm-IST rules engine. Used to be its own Briefing tab; now renders inside **Team ▸ Managers** with no title of its own. Page order there is Today's brief → Managers roster → Rulebook — the roster is passed in from `AdminPanel` as the `beforeRulebook` prop rather than rendered as a sibling, because Today's brief and the Rulebook share `rulesVersion` state. The daily-briefing on/off switch (`manager_settings.daily_briefings_enabled`) is a bare toggle in the "Today's brief" heading row.
- `src/supabase.ts`: fetchers/mappers. **`Event.id` = `events.slug`.**
- `src/JourneyMap.tsx` + `src/journeyMapSeeds.ts`: admin **"Build"** tab (tab key is still `'map'`) — React Flow user-journey maps backed by `journey_maps` (is_admin RLS). Page order is to-dos → map canvas last. Seeds file = "Reset map" baseline; when a flow changes, refresh the matching seed nodes. Dev preview without login: `npm run dev` + `/admin?mapdev`.
- `src/TodoCard.tsx`: standalone to-do list, rendered by `JourneyMap` at the **top** of the Build tab, above the map canvas (strict-admin only — ops can't see it; the gating prop is `showTodos`). To-dos live in `product_todos` (strict-admin RLS). **The Product Roadmap was removed on 2026-09-02** at the owner's request — `ProductRoadmap.tsx`, the `roadmap_features`/`roadmap_tasks`/`roadmap_test_runs` tables, and the `feature_releases` trigger `sync_release_to_roadmap()` are all gone, so a push to `main` no longer creates a "Need Testing" card. `feature_releases` itself REMAINS: it is the release log behind Growth ▸ Experiments and is still fed by the `log-release.yml` GitHub Action. Don't recreate the roadmap without asking.
- `supabase/functions/`: `create-payu-order` (server-trusted pricing + open-event payment gate), `payu-callback` (flips application status, fires WhatsApp, redirects), `payu-webhook`, `cart-abandonment` (30-min cron), `get-user-context` (phone → invites/applications/**doubts**/receipt, RLS bypass), `open-event-otp` (WhatsApp + email OTP for open-event bookings), `retarget-check`.

## Domain facts
- `events.booking_url`: `native-application` = invite-only · `payu-hosted` = open event · community events use `booking_flow='whatsapp'`. `booking_flow` is NOT NULL, CHECK (`payment`|`whatsapp`).
- `events.payment_mode`: `split` (advance + balance) | `full` (single payment → `fully_paid` directly).
- `applications.status`: `pending → invited → advance_paid → fully_paid` (+ `waitlist`/`rejected`). Open events have no approval: `pending` = "in progress". Display-only flags: `cart_abandoned`, `re_target`; `recovered_at` = paid after abandoning (badge, not a status).
- Unique key `applications(event_slug, phone)`; phones stored as last-10-digits.
- Per-date timelines: `event_dates.booking_steps` (JSONB, canonical 5 steps; index 2 = balance step, index 3 = meeting-spot step). Always prefer the applicant's `selected_date` steps over event-level fallback.
- RLS: anon cannot SELECT `applications`/`invited_numbers`/`invite_payment_submissions` — use `get-user-context` or the `event_booking_counts(_by_date)` RPCs.
- Marketers: `event_marketers` maps events→marketers; round-robin assignment trigger on application INSERT; commission accrues on `fully_paid`. Copied events inherit marketer mappings.
- **`applications` records current state, not history — several fields overwrite the past in place.** `assigned_marketer_id` re-stamps on unpaid leads when an event's marketers change; `selected_date` is mutable (a marketer rolling a lead to the next date leaves no trace); `cart_abandoned` is never cleared once set; slugs/titles drift on copied events; `affiliate_id` re-attributes at payment. **Any per-date or per-marketer cohort analysis of historical data is therefore unreliable** — both of those produced confidently wrong answers on 2026-08-07/08. Since 2026-08-08 the `application_events` table (trigger `trg_zz_log_application_change`, readable via the `application_history` view) records every change to status / selected_date / marketer / manager / affiliate / call_status / cart_abandoned / recovered_at / city / pickup with a timestamp and role. Append-only: is_admin_strict SELECT only, and INSERT/UPDATE/DELETE/**TRUNCATE** revoked from anon+authenticated (TRUNCATE bypasses RLS). Rows before 2026-08-08 are `field='baseline'` snapshots, NOT observed history. The trigger swallows all exceptions so logging can never roll back a booking.
- Commission attribution is **pinned to the booking row, never resolved live at payment time** — `applications.assigned_marketer_id` and `applications.assigned_manager_id`, both stamped on INSERT and read by the accrual triggers. This is what stops a late/bulk `fully_paid` flip from paying whoever happens to be assigned today (that bug hit managers live on 2026-08-02). Changing an event's marketers/manager re-stamps only leads whose `status NOT IN ('advance_paid','fully_paid','rejected')`; anything that took money keeps its original owner. One manager per event is enforced by `uq_event_managers_event`, so a handover is always remove-then-add.
- Open-event payment gate (`create-payu-order`): a NEW open-event ticket requires a verified `open_event_otp_sessions` token, EXCEPT two deliberate skips — a prior `payu_payments` row for this event+phone (failed-attempt **recovery** deeplink) or a matching `doubt_submissions` row (event + phone + email). An existing `applications` row is intentionally NOT accepted as proof (anon can self-INSERT a `pending` row). Balance payments never re-OTP. OTP rate limits are per-channel: WhatsApp 2/10min keyed by phone, email 2/10min keyed by email (email fallback also needs a valid prior WhatsApp session).

## Verification
- After every code edit: `npx tsc --noEmit` must pass.
- Preview server: **port 5175** (`vite.config.ts`, `strictPort: true` — the single source of truth; `npm run dev` must NOT pass its own `--port`). Since 2026-08-17 a launchd agent `~/Library/LaunchAgents/com.chaptera.vite-dev.plist` keeps it running at login and restarts it on crash, so the port is normally already taken — the in-app preview pane can only attach to servers it started itself, so to drive the pane run `launchctl bootout gui/$(id -u)/com.chaptera.vite-dev` first, then `preview_start`. Logs: `~/Library/Logs/chaptera-vite-dev.log`. Admin/marketer views sit behind login — not drivable in preview; verify those via tsc + SQL simulation instead.
- For DB changes: show the user exactly what changed (`RETURNING`), and re-check with a SELECT.

## Workflow preferences
- One concern per commit; commit messages explain the *why*.
- Ask before anything irreversible or outward-facing (pushes, deletes of non-test data, deploys).
- On "continue where you left off": consult the auto-memory note `open-event-flow-design` first instead of re-exploring code.
